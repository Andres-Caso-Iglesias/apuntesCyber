> [!info] Ficha tÃ©cnica
> **Instructor:** Carlos Castillo Â· **Fecha:** 20/07/2026
> **MÃ³dulo:** AuditorÃ­a web Â· PortSwigger Â· SSRF (Server-Side Request Forgery)
> **Entorno:** Kali Linux Â· Burp Suite Â· PortSwigger Labs
> **Participantes destacados:** Charchas, Mariana, Asier, Sira, Jon, Juan Carlos, Chema

> [!tip] CÃ³mo leer estos apuntes
> Esta sesiÃ³n introduce **SSRF** como la vulnerabilidad que completa el trÃ­ptico de "abuso de confianza del servidor" junto a Path Traversal y XXE. A diferencia de mÃ¡quinas completas, aquÃ­ se trabaja **la misma aplicaciÃ³n** con distintos niveles de sanitizaciÃ³n. El objetivo NO es completar laboratorios, sino construir el **mapa mental** que permita detectar y explotar SSRF en cualquier entorno real.

---

## â‘  SSRF â€” QuÃ© es y por quÃ© importa

### DefiniciÃ³n Core

**SSRF (Server-Side Request Forgery)** ocurre cuando una aplicaciÃ³n web hace una peticiÃ³n HTTP a una URL que puede ser controlada, total o parcialmente, por el atacante. La clave es que **la peticiÃ³n la hace el servidor, no el navegador del usuario**. El servidor tiene acceso a recursos que el atacante no puede alcanzar directamente: `localhost`, redes internas, servicios sin autenticaciÃ³n que solo escuchan en interfaces internas, y endpoints de metadatos de proveedores cloud.

### El Vector de DetecciÃ³n

Es el mismo que en Path Traversal: buscar en Burp Suite parÃ¡metros que contengan URLs o partes de URLs, como `url=`, `api=`, `endpoint=`, `redirect=`, `fetch=` o `resource=`. Cualquier parÃ¡metro que sugiera que el servidor va a hacer una peticiÃ³n a partir de ese valor es un candidato a SSRF.

- **La metÃ¡fora Ãºtil:** el servidor es el empleado con tarjeta de acceso que puede entrar en salas donde tÃº no puedes. Si consigues decirle al empleado a quÃ© sala ir y quÃ© traerte, no necesitas la tarjeta.

---

## â‘¡ Caso 1 â€” SSRF contra el propio servidor (`localhost`)

### El Escenario

La aplicaciÃ³n de la tienda tiene un panel de administraciÃ³n en `/admin` que **no estÃ¡ accesible desde el exterior**: solo responde a peticiones provenientes de `localhost`. Hay una funcionalidad de "comprobar stock" que hace una peticiÃ³n al backend usando un parÃ¡metro `stockApi` con la URL de la API interna.

### ExplotaciÃ³n

Al interceptar esa peticiÃ³n en Burp y modificar el valor por `http://localhost/admin`, el servidor hace la peticiÃ³n a sÃ­ mismo, y como el origen de la peticiÃ³n es el propio servidor, el panel de administraciÃ³n responde con el contenido completo. El response de la aplicaciÃ³n incluye el HTML del panel interno con los usuarios del sistema.

Para completar el laboratorio se encadena la peticiÃ³n: primero se obtiene la URL del endpoint de borrado de usuarios del HTML del panel, y luego se hace una segunda peticiÃ³n SSRF apuntando a esa URL:

```
stockApi=http://localhost/admin/delete?username=carlos
```

El servidor ejecuta la peticiÃ³n de borrado como si viniera del propio sistema, con todos los privilegios que eso implica.

> [!important] La idea clave
> Los paneles de administraciÃ³n que solo escuchan en `localhost` asumen que `localhost` es confiable. SSRF rompe esa asunciÃ³n porque el atacante puede hacer que el servidor hable con sÃ­ mismo.

---

## â‘¢ Caso 2 â€” SSRF contra infraestructura interna

### El Escenario

En este caso la aplicaciÃ³n hace peticiones a un servidor interno en la red `192.168.0.0/24`. El atacante no conoce quÃ© IP exacta ni quÃ© puerto expone el servicio administrativo. Se usa el **Intruder de Burp** para hacer un escaneo de hosts y puertos dentro de la red interna.

### MetodologÃ­a

1. Se itera sobre las IPs del rango `192.168.0.1` a `192.168.0.255`, buscando respuestas con cÃ³digo distinto de 404. La respuesta diferente delata el host activo.
2. Una vez identificada la IP, se itera sobre los puertos mÃ¡s comunes (en este laboratorio se da el rango 8000 a 9000 para acotar) buscando la misma diferencia de respuesta.
3. El panel administrativo encontrado responde a peticiones sin autenticaciÃ³n porque asume que nadie puede llegar a Ã©l desde el exterior. SSRF lo convierte en accesible.

> [!important] La idea clave
> Los servicios internos sin autenticaciÃ³n son el objetivo mÃ¡s valioso de SSRF. Asumen que la red interna es confiable, y SSRF convierte al servidor pÃºblico en un pivote hacia esa red.

---

## â‘£ Caso 3 â€” SSRF contra metadatos de AWS

### El Endpoint MÃ¡gico

En entornos cloud, las instancias EC2 de AWS exponen un endpoint de metadatos en la direcciÃ³n `169.254.169.254`. Esta es una direcciÃ³n **link-local** (no enrutable fuera de la instancia) que devuelve informaciÃ³n sobre la instancia sin ningÃºn tipo de autenticaciÃ³n: nombre de la instancia, regiÃ³n, rol de IAM asignado, y lo mÃ¡s crÃ­tico, las **credenciales temporales del rol**.

### NavegaciÃ³n del Ãrbol de Metadatos

```bash
# 1. Ãndice de categorÃ­as disponibles
stockApi=http://169.254.169.254/latest/meta-data/

# 2. Profundizar hasta credenciales IAM
http://169.254.169.254/latest/meta-data/iam/security-credentials/

# 3. Obtener nombre del rol asignado a la instancia
# 4. Ãšltima peticiÃ³n con el nombre del rol
http://169.254.169.254/latest/meta-data/iam/security-credentials/nombre-del-rol
```

La Ãºltima peticiÃ³n devuelve las credenciales temporales: `AccessKeyId`, `SecretAccessKey` y `Token`. Con estas tres credenciales se puede autenticar contra cualquier servicio de AWS con los permisos que tenga asignados el rol de la instancia.

> [!important] La idea clave
> El endpoint de metadatos de AWS es el objetivo mÃ¡s crÃ­tico de SSRF en entornos cloud. Las credenciales IAM obtenidas pueden dar acceso a S3, RDS, Lambda, o cualquier otro servicio que el rol tenga permitido. En auditorÃ­as de aplicaciones cloud, SSRF siempre debe apuntar a este endpoint.

---

## â‘¤ Bypasses de Filtros SSRF

Se cubrieron los bypasses mÃ¡s comunes para cuando el servidor intenta bloquear peticiones a direcciones prohibidas.

### Cuando el servidor bloquea `127.0.0.1` literalmente
- RepresentaciÃ³n decimal de la IP: `2130706433` (que es `127.0.0.1` en decimal)
- Forma abreviada: `127.1`
- Forma octal: `0177.0.0.1`
- Todas son representaciones vÃ¡lidas de `localhost` que algunos parsers no reconocen como equivalentes.

### Cuando el servidor bloquea `localhost` como palabra
- Usar `127.0.0.1` directamente, o viceversa.

### Cuando el servidor bloquea el dominio directamente
- Usar un dominio propio que resuelva a la IP bloqueada. Si el atacante controla un DNS, puede registrar un dominio que apunte a `127.0.0.1` y el servidor resolverÃ¡ el dominio sin saber que la IP es `localhost`.

### Cuando hay una redirecciÃ³n abierta en la aplicaciÃ³n
- Encadenar con SSRF: el parÃ¡metro vulnerable apunta a la URL de la redirecciÃ³n abierta, que a su vez redirige al destino interno. Si el servidor sigue redirecciones automÃ¡ticamente, llega al destino interno sin que el filtro de SSRF lo detecte.

---

## â‘¥ SSRF Ciego

### QuÃ© es

El **SSRF ciego** ocurre cuando el servidor hace la peticiÃ³n pero el resultado no aparece en el response de la aplicaciÃ³n. La peticiÃ³n se ejecuta en el backend, pero la respuesta queda en el servidor y el atacante no la ve directamente.

### DetecciÃ³n: Burp Collaborator

Un servidor externo controlado por el atacante que registra cualquier peticiÃ³n que reciba. Al apuntar el parÃ¡metro vulnerable a la URL del Collaborator, si el servidor hace la peticiÃ³n, el Collaborator la registra aunque el atacante no vea nada en el response de la aplicaciÃ³n. Esto confirma que hay SSRF aunque sea ciego.

### ExplotaciÃ³n

MÃ¡s compleja, se verÃ¡ en sesiones avanzadas: requiere encadenar con otras vulnerabilidades o usar tÃ©cnicas out-of-band para exfiltrar datos.

---

## â‘¦ RecapitulaciÃ³n Integrada

SSRF completa el cuadro de vulnerabilidades que convierten al servidor en aliado del atacante:

| Vulnerabilidad | QuÃ© hace el servidor por el atacante |
|----------------|--------------------------------------|
| **Path Traversal** | Lee ficheros locales |
| **XXE** | Parsea XML externo con consecuencias locales |
| **SSRF** | Hace peticiones de red que el atacante no puede hacer directamente |

â†’

Los tres vectores comparten la misma raÃ­z: una funciÃ³n del servidor acepta entrada de usuario sin validar y la usa para acceder a un recurso. La semana que viene se entra en **SQL Injection** siguiendo la misma estructura progresiva: desde los casos mÃ¡s simples hasta blind SQLi y tÃ©cnicas out-of-band.

---

## â‘§ Checklist Mental â€” SSRF

```
Â¿Veo un parÃ¡metro que sugiere que el servidor va a hacer una peticiÃ³n?
 â”‚
 â”œâ”€ Probar localhost / 127.0.0.1 / 169.254.169.254 / [::1]
 â”‚
 â”œâ”€ Si responde 200 + HTML admin â†’ SSRF confirmado, explotar
 â”‚
 â”œâ”€ Si responde 400/403 "blocked" â†’ Filtro activo (Blacklist)
 â”‚ â”œâ”€ Encoding (1x/2x/3x)
 â”‚ â”œâ”€ IP alternativa (127.1, decimal, octal, IPv6)
 â”‚ â”œâ”€ DNS trick (lvh.me, dominio propio â†’ 127.0.0.1)
 â”‚ â””â”€ Redirect chain
 â”‚
 â”œâ”€ Si timeout / 500 / sin cambio visible â†’ Blind SSRF candidate
 â”‚ â””â”€ Collaborator / Interactsh
 â”‚
 â””â”€ Si red interna expuesta â†’ Intruder para fuzzing IPs/puertos
```

> [!note] La regla de oro
> En cloud, **SIEMPRE** probar `169.254.169.254` (AWS) / `metadata.google.internal` (GCP) / `169.254.169.254` con header `Metadata: true` (Azure) como primer intento. SSRF + Metadata = Cloud Admin.

---

## â‘¨ Notas de Clase

- Esta sesiÃ³n la imparte **Carlos Castillo** y cubre la vulnerabilidad SSRF dentro del mÃ³dulo de PortSwigger.
- El hilo conductor es simple pero poderoso: **cuando un servidor hace peticiones a otras direcciones por cuenta propia, y esa direcciÃ³n puede ser controlada por el atacante, el servidor se convierte en un proxy involuntario** desde el que se puede alcanzar infraestructura interna, servicios en `localhost` y metadatos de cloud que normalmente son inaccesibles desde el exterior.
- La sesiÃ³n cubre los **tres contextos canÃ³nicos de SSRF**: contra el propio servidor, contra infraestructura interna, y contra servicios de metadatos de AWS.
- Se enfatiza la **metodologÃ­a progresiva**: entender la raÃ­z, ver el caso base sin filtros, analizar request/response, probar payloads simples, y si falla, analizar POR QUÃ‰ (mensaje de error, status code, length) para aplicar bypass lÃ³gico.
- **OpSec real:** Resource Pool a 1 req/seg no es paranoia, es profesionalidad. Un escaneo interno masivo desde SSRF = alerta en SIEM = te pillan = cliente enfadado = contrato perdido. Fragmentar: 20 IPs hoy, 20 maÃ±ana. Paciencia.
- **Sobre nombrar vulns:** En el informe pon el nombre que quieras: "Acceso indebido a panel admin interno", "SSRF en stock API", "Server-Side Request Forgery". Lo que IMPORTA es la explicaciÃ³n tÃ©cnica y el impacto. No os obsesionÃ©is con la etiqueta.
- **PrÃ³ximos pasos:** SQL Injection (misma estructura: bÃ¡sico â†’ union â†’ blind â†’ time-based â†’ OOB), Command Injection/RCE, Deserialization, API Security, Cloud Pentesting, Red Team/Pivoting (SSRF â†’ Internal â†’ Domain Admin).

â†’

â†’

---

> [!tip] La lecciÃ³n mÃ¡s importante
> "No os memorices payloads. Entiende la LÃ“GICA. Â¿Por quÃ© funciona 127.1? Porque el parser IP lo permite. Â¿Por quÃ© doble encoding? Porque validador decodifica 1 vez, backend N veces. Si entiendes el PORQUÃ‰, inventas tus propios bypasses."
> â€” Carlos Castillo
â†’
