
> [!info] Ficha tÃ©cnica
> **Instructor:** Carlos GÃ³mez Pintado (el coletas) Â· **Fecha:** 06/07/2026
> **MÃ³dulo:** IngenierÃ­a social / Anonimato / Fuzzing web
> **Entorno:** Kali Linux Â· MÃ¡quina RockStar de HackerLabs

> [!tip] CÃ³mo leer estos apuntes
> La sesiÃ³n mezcla tres bloques: **infraestructura anÃ³nima** (VPN, Monero, Mulvad, Tor, VPS, dominio), **ingenierÃ­a social avanzada** (QRishing, IP Logger, phishing con IA, ciberresponsabilidad bancaria) y **fuzzing de parÃ¡metros** con herramientas propias (x8, Python). El enfoque es defensivo: entender para detectar, atribuir y protegerse.

---

## â‘  Objetivos de la sesiÃ³n

- Entender cÃ³mo se construye una infraestructura anÃ³nima completa (VPN â†’ Monero â†’ VPS â†’ dominio) y por quÃ© la mayorÃ­a de VPNs comerciales no son seguras.
- Conocer el modelo **Crime-as-a-Service (CaaS)** y **Ransomware-as-a-Service (RaaS)** con sus tres roles y precios de mercado.
- Aprender a atribuir estafas usando IP logger y geolocalizaciÃ³n inversa.
- Conocer la **ciberresponsabilidad bancaria** y cÃ³mo recuperar dinero de estafas.
- Comprender la **triangulaciÃ³n de seÃ±ales** y cÃ³mo se geolocaliza una IP.
- Dominar el **fuzzing de parÃ¡metros** para encontrar endpoints ocultos en aplicaciones web.
- Crear herramientas propias en Python para fuzzing cuando las herramientas existentes fallan.

---

## â‘¡ Conceptos clave

| Concepto | DefiniciÃ³n |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **VPN** | Virtual Private Network. TÃºnel cifrado hacia servidores alquilados en CPD. Funciona como proxy n-a-1: muchos usuarios, IPs rotatorias, un operador con las tablas de enrutado. |
| **Proxy** | Servidor intermediario con IP fija (1 usuario, 1 IP). Una VPN es como un proxy pero con N IPs que cambian en cada reconexiÃ³n. |
| **Tabla de enrutado** | Correlaciona IP real del usuario con IP de VPN asignada en cada timestamp. Las VPNs comerciales guardan esto 5+ aÃ±os y lo entregan con orden judicial de FBI, Interpol, Hacienda, CNI. |
| **Mulvad** | VPN sin logs, no nominal (identificador = nÃºmero de cuenta generado localmente, sin dato personal). Acepta Monero. Ha rechazado 17+ solicitudes de colaboraciÃ³n gubernamental. Incluye navegador + VPN. |
| **Monero (XMR)** | Criptomoneda sin trazabilidad en blockchain. Wallets no son pÃºblicamente correlacionables. La "moneda de los delincuentes" por excelencia. |
| **MetaMask** | ExtensiÃ³n de Chrome para gestionar criptomonedas. No requiere usuario ni contraseÃ±a â€” solo una extensiÃ³n y una seed phrase de 12-24 palabras. |
| **Seed phrase** | 12 a 24 palabras aleatorias en inglÃ©s que constituyen la Ãºnica forma de recuperar una wallet. Si se pierde, se pierde todo. |
| **Uniswap** | DEX (decentralized exchange) para hacer swap de tokens. USDT â†’ XMR sin trazabilidad. |
| **Tor** | Red de nodos en capas (como cebolla). Cada nodo solo conoce el anterior y el siguiente. Los nodos de salida estÃ¡n controlados en 90%+ por agencias gubernamentales. |
| **Njala** | Proveedor de dominios, VPS y servidores que acepta Monero. Ãštil para hostear infraestructura anÃ³nima. |
| **Calentar dominio** | Enviar correos progresivamente (~1 mes) con Smart Lead para subir domain authority y evitar que Google/Microsoft lo marquen como sospechoso. |
| **Phishing / QRishing** | IngenierÃ­a social por web falsa / mediante cÃ³digos QR maliciosos. El QRishing es la evoluciÃ³n del phishing. |
| **IP Logger** | Servicio que registra IP, dispositivo, SO y geolocalizaciÃ³n tras un clic. Ãštil para atribuciÃ³n de estafadores. |
| **Smart Data vs GPS** | IP Logger puede capturar solo IP (Smart Data, siempre funciona) o GPS (requiere permiso del usuario, menos probable que acepte). |
| **CaaS** | Crime-as-a-Service: el hacker vende infraestructura y herramientas de forma legal. |
| **RaaS** | Ransomware-as-a-Service: uno develop el ransomware, otro compra acceso inicial, otro ejecuta. |
| **Ciberresponsabilidad bancaria** | ObligaciÃ³n del banco de devolver dinero a vÃ­ctimas de ciberestafas si se adjunta denuncia + movimientos bancarios sellados. |
| **TriangulaciÃ³n** | TÃ©cnica para geolocalizar: el terreno estÃ¡ dividido en hexÃ¡gonos, cada uno con una torre. Con las 3 Ãºltimas conexiones de tu SIM se determina la zona comÃºn. |
| **TTL (Time To Live)** | Valor en el ping que revela el SO: 64=Linux, 128=Windows, 192=Solaris, 254=Mac (va de 64 en 64). |
| **Fuzzing de parÃ¡metros** | TÃ©cnica para descubrir parÃ¡metros ocultos en URLs enviando valores y analizando las respuestas HTTP. |
| **x8** | Herramienta para fuzzing de parÃ¡metros. Se instala con Docker o compila con Cargo (Rust). Docker falla frecuentemente. |

---

## â‘¢ Infraestructura anÃ³nima completa

> [!warning] Enfoque defensivo
> Se describe la arquitectura que usan los ciberdelincuentes para que sepas identificarla, atribuirla y protegerte. No es una guÃ­a para replicarla.

### Capa 1: VPN anÃ³nima

Las VPNs comerciales (NordVPN, Surfshark, ProtonVPN, ExpressVPN, Checkpoint, TunnelBear) **colaboran con gobiernos** (Hacienda, CNI, FBI, Interpol). Mantienen tablas de enrutamiento internas que correlacionan cada IP pÃºblica asignada con la IP real del usuario y el timestamp exacto. Sin esas tablas el trÃ¡fico no podrÃ­a ser devuelto al usuario. Se conservan aÃ±os y se entregan ante requerimiento judicial.

**Â¿CÃ³mo funciona una VPN internamente?**

```
1. Usuario (IP real) â†’ envÃ­a peticiÃ³n
2. VPN recibe â†’ la envÃ­a a travÃ©s del proxy al servidor destino
3. Servidor destino â†’ devuelve respuesta
4. VPN recibe â†’ consulta tabla de enrutado (IP real â†” IP VPN en timestamp)
5. Devuelve la respuesta al usuario original
```

La tabla de enrutado es **sÃºper peligrosa**: sin ella, la VPN no podrÃ­a funcionar (es imposible redirigir trÃ¡fico sin saber a quiÃ©n devolverlo). Pero es exactamente lo que permite desanonimizar al usuario.

> [!important] Concepto clave
> Una VPN es funcionalmente un **proxy n-a-1** (muchos usuarios, muchas IPs rotatorias, un operador con las tablas). La diferencia entre una VPN anÃ³nima y una comercial no es tÃ©cnica sino **legal y de polÃ­tica de datos**: quiÃ©n tiene acceso a la correlaciÃ³n IP real / IP asignada.

**Mulvad** es la excepciÃ³n: no guarda logs, no es nominal (el identificador es un nÃºmero de cuenta generado localmente, sin dato personal asociado), acepta Monero. Ha rechazado 17+ solicitudes de colaboraciÃ³n gubernamental documentadas. Incluye navegador web + VPN. Precio desde 1â‚¬ (con descuento irÃ³nico: "cuanto mÃ¡s compras, menos ahorras").

**MÃ©todos de pago de Mulvad:**
- ~~Efectivo~~ (ya no disponible)
- CupÃ³n comprado por ahÃ­
- Tarjeta de crÃ©dito (te identifica â€” **no hacerlo**)
- **Monero** (la mejor opciÃ³n)

> [!tip] AnalogÃ­a legal
> "Â¿Es legal Mulvad? SÃ­. Â¿Es legal comprar una katana en Toledo? SÃ­. Â¿Es legal atravesar a una persona con ella? No. Mulvad es una herramienta. La cosa es cÃ³mo la utilizas."

### Capa 2: Monero para trazabilidad cero

Monero (XMR) es la criptomoneda preferida porque **no guarda trazabilidad en blockchain**: las wallets no son pÃºblicamente correlacionables.

**Flujo para obtener Monero anÃ³nimamente:**

```
Binance (compra USDT con tarjeta)
 â†“ trazable aquÃ­ (Binance lleva 3 aÃ±os con Hacienda)
MetaMask wallet 1
 â†“ trazable aquÃ­
Uniswap (swap USDT â†’ XMR)
 â†“ SIN trazabilidad aquÃ­
MetaMask wallet 2 â† ESTA ES ANÃ“NIMA
 â†“
Mulvad VPN (paga con XMR)
```

En cada salto se pierde un eslabÃ³n de trazabilidad. La wallet 2 ya es completamente anÃ³nima.

**Alternativa sin Binance:** P2P directo con alguien de confianza ("Monchis"). Le das 50â‚¬ en cash, Ã©l te envÃ­a 50â‚¬ en USDT desde su wallet a la tuya. Sin rastro en exchanges. Incluso mÃ¡s limpio: cafÃ© + transferencia directa.

**Otros exchanges:** Bit2Me, eToro, OKX (la china).

**Trocador:** Herramienta mencionada por Nacho para cambiar Bitcoin â†’ XMR y XMR â†’ efectivo. "Lo que Nacho diga va a misa."

**Monero se puede obtener tambiÃ©n via:** Bit2Me, eToro, OKX, o directamente de conocidos.

### Capa 3: VPS anÃ³nimo

- Comprar VPS con Monero en **Enjala** (proveedor de dominios + VPS + servidores que acepta cripto).
- **Sin VPS anÃ³nimo, toda la infraestructura anterior no sirve**: si el servidor tiene IP externa, aparece nuestro DNI.
- Conectar al VPS Ãºnicamente con **clave pÃºblica RSA** inyectada al contratar (eliminando usuario y contraseÃ±a).
- El VPS tambiÃ©n va detrÃ¡s de Mulvad.

```bash
# ConexiÃ³n anÃ³nima al VPS (sin user/pass, solo clave SSH)
ssh root@<ip_del_vps> -i <ruta_a_la_key_privada>
```

**Â¿Por quÃ© SSH con clave RSA?** Porque user/pass genera registros. La clave RSA no tiene usuario asociado.

> [!tip] MetÃ¡fora
> Es como construir una casa con materiales que no tienen nÃºmero de serie. Cada capa individual podrÃ­a ser trazable si hubiera un registro, pero si ninguna de las capas lo tiene, no hay punto de partida para la investigaciÃ³n.

### Capa 4: Dominio anÃ³nimo

- Comprar dominio en **Enjala** con Monero (NO en GoDaddy/Hostinger con tarjeta â€” "Oscar ha decidido mandarlo a la mierda, ha dicho: mira quÃ© anÃ³nimo soy, toma mi tarjeta y mi DNI").
- **Calentar el dominio** durante ~1 mes con **Smart Lead**: enviar correos entre diferentes emails para subir domain authority. Si no lo calientas, Google y Microsoft lo marcan como "dominio de reciente creaciÃ³n" y lo bloquean.
- AÃ±adir SSL (candadito) para que no salten alertas de seguridad en el navegador.

### Resultado

Infraestructura 100% anÃ³nima por **< 100â‚¬/aÃ±o**: VPN + VPS + dominio + SSL, todo pagado con Monero. "Por menos de lo que te cuesta tres cafÃ©s de especialidad en el Starbucks y un frappuccino."

**Â¿Se puede practicar?** SÃ­. Chema recomienda: "Coged, metedos a Discord, que uno pague Mulvad, otro el VPS y otro un dominio. Os montÃ¡is cualquier tonterÃ­a para vender servidores de Minecraft o GTA 6. Y asÃ­ practicÃ¡is."

### Tor: Â¿Sirve?

Tor funciona por capas (de ahÃ­ la analogÃ­a con la cebolla): el trÃ¡fico salta a travÃ©s de varios nodos antes de salir a Internet, y cada nodo solo conoce el anterior y el siguiente.

**El problema:** Los **nodos de salida** (los que ven el trÃ¡fico descifrado hacia el destino) estÃ¡n controlados en un **90%+ por agencias gubernamentales** (principalmente FBI). Los nodos de salida necesitan una hoja de enrutamiento para devolver la respuesta al origen, lo que permite correlacionar trÃ¡fico de entrada y salida y desanonimizar al usuario.

Tor tiene sentido como **capa adicional** dentro de una infraestructura VPS privada (hostear en .onion, sin DNS pÃºblico), pero como soluciÃ³n autÃ³noma de anonimato es **insuficiente** en el contexto actual. "Puedes utilizar Tor, sÃ­, estÃ¡ rarillo si tienes la suerte del 2%, pero con Mulvad + VPS + Monero es mucho mejor."

---

## â‘£ Crime-as-a-Service (CaaS) y Ransomware-as-a-Service (RaaS)

### Modelo actual del cibercrimen

El cibercrimen profesional moderno **no funciona como un hacker individual**. Funciona como un mercado de especializaciÃ³n con tres roles claros:

```
Cadena de valor del RaaS:
â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
â”‚ 1. Desarrollador de ransomware â”‚
â”‚ â†’ Vende malware: 500.000â‚¬ + 10% del rescate â”‚
â”‚ â†’ No se arriesga, opera desde la legalidad aparente â”‚
â”‚ â†’ "Vende software" â”‚
â”œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¤
â”‚ 2. Broker de acceso inicial â”‚
â”‚ â†’ Consigue credenciales via phishing/RCE â”‚
â”‚ â†’ Vende acceso: ~100.000$ â”‚
â”‚ â†’ Mucho menos riesgo que desplegar ransomware â”‚
â”œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¤
â”‚ 3. Operador final (el "cliente") â”‚
â”‚ â†’ Compra acceso + ransomware â”‚
â”‚ â†’ Ejecuta con doble clic â”‚
â”‚ â†’ Negocia rescate â”‚
â”‚ â†’ Asume TODO el riesgo â”‚
â”‚ â†’ Puede tener cero conocimiento tÃ©cnico â”‚
â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜
```

> [!important] Idea clave
> El que estafa no es el hacker. El hacker vende infraestructura y herramientas. Quien las usa para delinquir es el comprador. Esto explica por quÃ© las estafas masivas (SMS de la DGT, phishing bancario) las ejecutan personas sin conocimiento tÃ©cnico que han comprado un kit completo.

**Â¿Por quÃ© es mÃ¡s rentable vender que ejecutar?**

| Escenario | Ingreso | Riesgo |
|-----------|---------|--------|
| Vender ransomware a 200 personas Ã— 2.000â‚¬ | 400.000â‚¬ | MÃ­nimo (vendes software) |
| Ejecutar 100 estafas individuales | Variable | 100 denuncias potenciales |

**Negocio adicional del cibercrimen:**
- Bases de datos de telÃ©fonos: 5M nÃºmeros Ã— 0.10â‚¬ = **500.000â‚¬**
- Servicios de anonimato: **10.000â‚¬**
- ReparaciÃ³n de plataformas: **20.000â‚¬**
- "Es mucho mÃ¡s rentable vender y mantener un software que utilizarlo."

**El cibercrimen ya no es de los hackers.** "Ahora es de los matados que saben que con internet se puede hacer mucho dinero." La gente que estafa no tiene conocimiento tÃ©cnico â€” por eso no usa VPN y es fÃ¡cil de atribuir.

### El "cliente" del hacker

- Persona con **cero conocimiento tÃ©cnico**.
- Compra infraestructura configurada (phishing + dominio + VPS).
- Solo hace "doble clic" para ejecutar.
- Es a quien la policÃ­a detiene, no al hacker.
- Ejemplo real: estafas de DGT donde la contraseÃ±a del cpanel era `admin` sin contraseÃ±a (la por defecto).

---

## â‘¤ IngenierÃ­a social avanzada

### QRishing (evoluciÃ³n del phishing)

El phishing por SMS y email tiene tasas de detecciÃ³n crecientes. El vector emergente es el **QRishing**: cÃ³digos QR fÃ­sicos colocados en lugares pÃºblicos que redirigen a pÃ¡ginas de login falsas.

**Â¿Por quÃ© funciona tan bien?**
- La gente estÃ¡ **condicionada a escanear QRs sin cuestionarlos** (cartas de restaurantes, carteles informativos, oficinas).
- Un QR con diseÃ±o visual idÃ©ntico al original, SSL activado y dominio calentado **no levanta ninguna alerta** en el navegador.
- El QR es **socialmente neutro**: la gente no aplica el mismo nivel de sospecha a un QR fÃ­sico que a un enlace en un SMS.

**Ejemplo prÃ¡ctico de clase (Red Team real):**
1. Objetivo: restaurante "El Goico" (frecuentado por empleados de la empresa objetivo).
2. Crear QR personalizado con colores y logos del restaurante.
3. Pegarlo en una mesa del restaurante real.
4. Al escanearlo â†’ formulario de login de Microsoft ("Goico por Mace â€” obtener descuento").
5. El usuario da a "login con Microsoft" (Ãºnica opciÃ³n), autocompleta credenciales del almacÃ©n de claves.
6. Resultado: usuario + contraseÃ±a robados.

> [!tip] Ejemplo real
> "Tengo un colega que lo ha hecho en un restaurante en Toledo, de su pueblo, que es el restaurante que lleva todo el mundo, y ahora mismo tiene cuenta de Instagram de todo su pueblo."

**Flujo tÃ©cnico del QRishing:**
1. Generar QR â†’ infraestructura anÃ³nima (VPS con Mulvad).
2. Alojar pÃ¡gina que replica el login del objetivo.
3. **ClonaciÃ³n de pÃ¡ginas**: cÃ³digo JavaScript que se pega en la consola del navegador (F12) y descarga toda la web (HTML + imÃ¡genes + vÃ­deos) en un ZIP. "Lo tengo en el curro. Con el tema de campaÃ±as de phishing, lo aprovecho para robar la web al cliente."
4. Credenciales introducidas se capturan y almacenan en el VPS.
5. Dominio calentado con **Smart Lead** (~1 mes) â†’ no aparece como sospechoso.

**Herramientas para clonar pÃ¡ginas:**
- CÃ³digo JS en consola del navegador (F12) â†’ descarga HTML + imÃ¡genes + vÃ­deos en ZIP.
- **Lovable** / **Emergent** â†’ clonan pÃ¡ginas desde URL.
- ChatGPT / Claude â†’ generan prompts para formularios de login falsos.
- "Con fines educativos, siempre lo digo."

**Siguiente sesiÃ³n:** Castillo enseÃ±arÃ¡ a integrar infraestructura con **GoPhish**, calentar dominios y usar templates de phishing. TambiÃ©n se mencionÃ³ **ProfPoint** para campaÃ±as de phishing (pero es "un coÃ±azo").

### IP Logger para atribuciÃ³n

TÃ©cnica defensiva para identificar estafadores usando ingenierÃ­a inversa:

1. Crear enlace en **IPLogger** con redirect a web legÃ­tima (ej: Santander).
2. **ConfiguraciÃ³n clave:**
 - **Recopilar Smart Data** (siempre funciona): IP, dispositivo, SO.
 - **NO GPS** (solicita ubicaciÃ³n y la gente no acepta).
 - **NO notificaciones**.
 - Nombrar el archivo realista: `transferencia.jpg` o `transferencia.pdf`.
 - IPLogger "canta mucho" â†’ mejor poner un dominio raro como `mapper.io`.
3. Enviar al estafador como "comprobante de transferencia".
4. Al clicar â†’ obtienes IP + dispositivo + SO + geolocalizaciÃ³n GPS (si concede permisos).

> [!tip] Dato clave
> La gente que estafa **no usa VPN** (no tienen conocimiento tÃ©cnico). "Si no, no hubiesen comprado esta estupidez." Por eso funciona: su IP real queda expuesta al clicar el enlace.

**GeolocalizaciÃ³n por IP:** No es GPS exacto, pero da una zona aproximada. "SabÃ©is cÃ³mo funciona lo de geolocalizar un IP? El terreno mundial estÃ¡ dividido en hexÃ¡gonos."

**Si el estafador usa VPN y cambia de IP:** "Le tiras a la policÃ­a el listado de IPs que te han salido y que hagan ellos." TÃº no haces nada mÃ¡s.

**Â¿QuÃ© sirve de verdad?** "De nada te vale sacar 100 IPs y 100 localizaciones. Lo que te vale es poner un formulario de login delante para sacar nombres de usuario y contraseÃ±a."

### Ciberresponsabilidad bancaria

Si eres vÃ­ctima de una ciberestafa (phishing de DGT, transferencia falsa, tienda online fantasma):

1. Ir a la policÃ­a y hacer denuncia.
2. Pedir al banco los **movimientos bancarios sellados**.
3. Adjuntar **denuncia + movimientos sellados** al banco.
4. El banco **se tiene que responsabilizar** de devolver el dinero.

> [!important] Derecho
> Los bancos tienen **ciberresponsabilidad** que va ligada a un **ciberseguro**. Si adjuntas la denuncia de la policÃ­a + movimientos bancarios sellados, el banco se responsabiliza y usted tiene que pagar ese dinero.

### Phishing con IA

Se puede usar IA para:
- **GPT Image 2** (con suscripciÃ³n Pro): genera imÃ¡genes ilimitadas, incluyendo clones de formularios de login.
- **Claude**: genera prompts detallados para clonar pÃ¡ginas web ("un prompt de tres pÃ¡rrafos explicando a cada detalle cÃ³mo quiere la pÃ¡gina").
- **Lovable / Emergent**: clonan pÃ¡ginas completas desde URL o capturas de pantalla.
- "La gente es imbÃ©cil y por ahorrarse 4â‚¬ te vende a su empresa."

---

## â‘¥ Redes: conceptos que aparecieron en la clase

### TTL y geolocalizaciÃ³n

El valor TTL (Time To Live) en un ping revela el sistema operativo del host remoto:

| TTL | Sistema Operativo |
|-----|-------------------|
| 64 | Linux |
| 128 | Windows |
| 192 | Solaris |
| 254 | Mac |

(Sempre va de 64 en 64.)

### TriangulaciÃ³n de seÃ±ales

El terreno mundial estÃ¡ dividido en **hexÃ¡gonos**, cada uno con una torre de telefonÃ­a (~20km de radio). Tu tarjeta SIM se conecta a ~3 torres simultÃ¡neamente.

**CÃ³mo funciona:**
1. Cuando estÃ¡s en un sector, te conectas automÃ¡ticamente a la torre de ese hexÃ¡gono.
2. Cuando te mueves (ej: en coche por la vÃ­a), cambias de sector y te conectas a otra torre.
3. Las 3 Ãºltimas conexiones de tu SIM se usan para **triangular** tu posiciÃ³n: la zona comÃºn a las 3 torres es donde estuviste.
4. Todo queda registrado con "hoja de traza".

"Es muy de pelÃ­cula el triangular la seÃ±al, pero en realidad es buscar las tres torres."

### Netmasks y rangos de red

Cuando se hace `netdiscover -r 10.0.2.0/24`, la `/24` indica cuÃ¡ntos octetos son comunes:

| Netmask | Octetos comunes | Rango | Ejemplo |
|---------|----------------|-------|---------|
| /24 | 3 octetos | Ãšltimo varÃ­a (0-255) | 10.0.2.0/24 |
| /16 | 2 octetos | Ãšltimos 2 varÃ­an | 10.0.0.0/16 |
| /8 | 1 octeto | Ãšltimos 3 varÃ­an | 10.0.0.0/8 |
| /0 | 0 octetos | Todos varÃ­an | 0.0.0.0/0 |

**Â¿Por quÃ© `0.0.0.0` en listeners?** Porque es un /0: escucha en TODAS las IPs del mundo. "Cuando hacemos Netcat -lvp 4444, estamos en 0.0.0.0:4444 â€” cualquier IP que nos llame en ese puerto la escuchamos."

### Amazon y ICMP

Amazon AWS **no tiene ICMP habilitado por defecto** en su infraestructura. "De esta manera podemos evitar que se haga la famosa ping sweep, que significa barrido de ping a toda la infraestructura de red para hacer descubrimiento de activos." Ciberseguridad por oscuridad.

---

## â‘¦ Fuzzing de parÃ¡metros HTTP

### Â¿QuÃ© es fuzzing?

Fuzzing = enviar valores iterados a un parÃ¡metro y analizar las respuestas HTTP para encontrar comportamientos anÃ³malos que revelen informaciÃ³n oculta.

> [!tip] MetÃ¡fora
> Es como llamar a todas las puertas de un edificio con llaves diferentes. La mayorÃ­a dan error. La que abre es el parÃ¡metro correcto.

### Flujo de trabajo en la mÃ¡quina RockStar

```
1. nmap -sC -sV -p- <IP> --min-rate 1500
 â†’ Puertos: 22 (SSH), 80 (HTTP/Apache)
 â†’ SSH necesita credenciales â†’ no brute force todavÃ­a

2. nikto -h <IP>
 â†’ Vulnerabilidades del servidor
 â†’ VersiÃ³n de Apache no vulnerable (descartamos exploit directo)

3. gobuster dir -u <IP> -w /usr/share/wordlists/dirb/common.txt
 â†’ Directorios: /js (acceso denegado)

4. feroxbuster -u <IP> -x php,py,html,txt
 â†’ Ficheros con extensiones:
 - index.php (muestra "yo no soy tu marido" â€” tiene printf, ejecuta cÃ³digo)
 - db.php (respuesta vacÃ­a, 0 bytes â€” ejecuta en backend sin output)

5. curl -X POST http://<IP>/index.php -d "test=1"
 â†’ Probar parÃ¡metros genÃ©ricos
 â†’ La mayorÃ­a devuelven 500

6. x8 / Python fuzzer â†’ descubrir parÃ¡metro "backdoor"
 â†’ Fuzzing del parÃ¡metro: backdoor=db.php â†’ cÃ³digo 200

7. Obtener credenciales â†’ SSH access â†’ root
```

**Â¿Por quÃ© `db.php` devuelve 0 bytes?** "Cuando nosotros hemos encontrado un fichero PHP, lo que estamos haciendo es ejecutar el PHP. Si el programa no tiene un printf para mostrarme algo por pantalla, la ejecuciÃ³n resulta en 0 bytes â€” pantalla en blanco. Se ha ejecutado en el backend."

### Herramienta: x8

Herramienta estÃ¡ndar para fuzzing de parÃ¡metros. Se puede instalar con Docker o compilar con **Cargo** (Rust):

```bash
# OpciÃ³n 1: Docker (falla frecuentemente)
git clone <repo-x8>
cd x8
docker build -t x8 .

# OpciÃ³n 2: Cargo (mÃ¡s fiable)
cargo install x8
# o desde el repo:
cargo build --release
```

> [!warning] Docker falla
> "Docker es una autÃ©ntica mierda. Veis por quÃ© os digo que no utilicÃ©is Docker? Porque no funciona para nada." La alternativa es compilar con Cargo o crear el fuzzer en Python.

### Fuzzer propio en Python (cuando x8 falla)

Chema creÃ³ el fuzzer durante la clase con Claude:

```python
# value_analyzer.py
# EnvÃ­a diccionario de parÃ¡metros y analiza respuestas HTTP
# Detecta transiciones 500 â†’ 200 como indicador de parÃ¡metro vÃ¡lido

import requests
import sys

url = sys.argv[1]
wordlist = sys.argv[2]

with open(wordlist) as f:
 for line in f:
 param = line.strip()
 r = requests.post(url, data={param: "test"})
 if r.status_code == 200:
 print(f"[+] Found: {param} (200 OK)")
```

**Uso:**
```bash
python3 value_analyzer.py http://10.0.2.9/index.php /usr/share/wordlists/dirb/common.txt
```

**Resultado:** El parÃ¡metro `backdoor` devolviÃ³ cÃ³digo 200. Luego se fuzzing el valor: `backdoor=db.php` â†’ credenciales de usuario.

> [!important] Concepto clave
> Un fichero PHP puede aceptar parÃ¡metros que no estÃ¡n documentados en ningÃºn formulario ni en la URL. Sin fuzzing de parÃ¡metros, esos vectores son invisibles. El cambio en el cÃ³digo de respuesta HTTP es la seÃ±al de que hemos encontrado algo.

### Diccionarios importantes

| Diccionario | Uso | Ruta |
|-------------|-----|------|
| `common.txt` | Directorios y ficheros web | `/usr/share/wordlists/dirb/common.txt` |
| `big.txt` | Directorios ampliado | `/usr/share/wordlists/dirb/big.txt` |
| `rockyou.txt` | **ContraseÃ±as** (NO para directorios) | `/usr/share/wordlists/rockyou.txt` |
| `medium-low` (dirsearch) | Directorios (mismo que dirb common) | Interno de dirsearch |

> [!important] Regla OSCP
> RockYou es de contraseÃ±as, NO sirve para directorios. Los wordlists de directorios son para fuzzing web, no para brute force SSH. "Siempre hay que saber quÃ© diccionario vale para quÃ©."

### Diferencia entre herramientas de enumeraciÃ³n

| Herramienta | FunciÃ³n | Diccionario por defecto | Nota |
|-------------|---------|------------------------|------|
| **gobuster** | EnumeraciÃ³n de directorios | dirb common | RÃ¡pido, popular |
| **dirsearch** | EnumeraciÃ³n de directorios | medium-low | Tiene medium-low propio |
| **feroxbuster** | Directorios + ficheros con extensiones (`-x`) | dirb common | El mÃ¡s versÃ¡til |
| **dirb** | EnumeraciÃ³n bÃ¡sica | dirb common | El mÃ¡s bÃ¡sico |

**Diferencia clave:** `dirsearch` y `gobuster` usan diccionarios diferentes por defecto. Si les pones el mismo, sacan lo mismo. Se usan juntas para que una saque lo que la otra no.

---

## â‘§ MÃ¡quina Rockstar (prÃ¡ctica â€” HackerLabs)

### Contexto

MÃ¡quina descargada en una sesiÃ³n anterior. Se resolviÃ³ con Mariana "hand to hand" durante la clase. El resto de compis tuvieron 20 minutos para intentarla.

### Descubrimiento de host

```bash
# Descubrir IPs en el rango de red
sudo netdiscover -r 10.0.2.0/24

# Resultado: 10.0.2.9 (vÃ­ctima potencial)
# La .2 suele ser interfaz de VirtualBox (router virtualizado)

# Verificar conectividad
ping 10.0.2.9

# TTL revela SO: 64 = Linux
# (128=Windows, 192=Solaris, 254=Mac, va de 64 en 64)
```

### EnumeraciÃ³n de puertos

```bash
nmap -sC -sV -p- 10.0.2.9 --min-rate 1500

# Resultado:
# Puerto 22: SSH (necesita credenciales â€” no brute force todavÃ­a)
# Puerto 80: HTTP (Apache)
```

**Â¿Por quÃ© no vamos directo al 22?** "Porque SSH necesitarÃ­amos tener una credenciales. Como no nos metamos ahora mismo directamente a pegarnos cabezazos contra una fuerza bruta, no podemos hacer nada. Lo Ãºnico que podrÃ­amos ver es si esta versiÃ³n tiene algÃºn CVE, pero por lo general un CTF nunca lo va a tener."

### EnumeraciÃ³n web

```bash
# Nikto - vulnerabilidades del servidor
nikto -h 10.0.2.9
# Apache no es vulnerable â†’ descartamos exploit directo

# Gobuster - directorios
gobuster dir -u 10.0.2.9 -w /usr/share/wordlists/dirb/common.txt
# Resultado: /js (acceso denegado)

# Feroxbuster - ficheros con extensiones
feroxbuster -u 10.0.2.9 -x php,py,html,txt
# Resultado: index.php, db.php
```

### AnÃ¡lisis de archivos encontrados

- **`index.php`**: Muestra "yo no soy tu marido" â†’ tiene `printf`, ejecuta cÃ³digo en el servidor.
- **`db.php`**: Respuesta vacÃ­a (0 bytes) â†’ ejecuta cÃ³digo en backend sin output visible. "Si el programa no tiene un printf para mostrarme algo por pantalla, la ejecuciÃ³n resulta en 0 bytes."

### Fuzzing de parÃ¡metros con curl

```bash
# Probar parÃ¡metro genÃ©rico
curl -X POST http://10.0.2.9/index.php -d "test=1"
# â†’ 500 Internal Server Error

# Probar con interrogante
curl "http://10.0.2.9/index.php?test=1"
# â†’ 500 Internal Server Error

# Probar parÃ¡metro "backdoor"
curl -X POST http://10.0.2.9/index.php -d "backdoor=db.php"
# â†’ 200 OK (Â¡parÃ¡metro encontrado!)

# Probar con data
curl -X POST http://10.0.2.9/index.php -d "data=backdoor"
# â†’ 500 (este formato no funciona)
```

### Fuzzer Python (creado por Asier en clase)

```python
# value_analyzer.py
import requests
import sys

url = sys.argv[1]
wordlist = sys.argv[2]

with open(wordlist) as f:
 for line in f:
 param = line.strip()
 r = requests.post(url, data={param: "test"})
 if r.status_code == 200:
 print(f"[+] Found: {param} (200 OK)")
```

```bash
python3 value_analyzer.py http://10.0.2.9/index.php /usr/share/wordlists/dirb/common.txt
# Resultado: backdoor â†’ 200 OK
```

### ResoluciÃ³n

1. ParÃ¡metro encontrado: `backdoor`
2. Valor del parÃ¡metro: `db.php`
3. Respuesta: credenciales de usuario
4. Acceso por SSH con esas credenciales
5. Escalada de privilegios â†’ root

---

## â‘¨ Notas importantes de la clase

### Sobre anonimato

- El anonimato "hace que la gente se crezca" â€” dice cosas que no dirÃ­a a la cara.
- Reutilizar alias/nick = reutilizar contraseÃ±a â†’ rompe el anonimato. "Siempre pasa lo mismo. La comodidad es lo mismo que nos conlleva a reutilizar contraseÃ±as."
- Si vas a hacer algo "anÃ³nimo": **cuenta nueva + IP nueva**. Siempre.
- "El anonimato es esa fantÃ¡stica y fabulosa cualidad que nos permite a los seres humanos ser subnormales."

### Sobre VPNs

- NordVPN, Surfshark, ProtonVPN, ExpressVPN, Checkpoint, TunnelBear = cooperan con gobiernos.
- **Mulvad** = Ãºnica VPN que ha rechazado colaboraciones gubernamentales (17+ casos).
- Las VPNs guardan tablas de enrutado 5+ aÃ±os (IP real â†” IP VPN en cada timestamp).
- "Una VPN no es mÃ¡s que un proxy, lo que veÃ­amos con Burp."

### Sobre Monero y MetaMask

- Monero no guarda trazabilidad en blockchain.
- MetaMask: extensiÃ³n Chrome, no requiere usuario/contraseÃ±a, solo seed phrase de 12-24 palabras.
- "Si pierdes esto, esta es la Ãºnica manera de recuperar tu billetera. La Ãºnica manera."
- Las wallet de MetaMask no estÃ¡n ligadas a nada â€” solo una extensiÃ³n y una frase.
- Cuidado con los tokens scam en MetaMask: "Este que tiene una 'A' delante, cuidado, pues es scam."
- Se puede crear la wallet desde el VPS con Mulvad para mantener anonimato total.

### Sobre Tor

- 90%+ de nodos de salida son de agencias gubernamentales (FBI).
- Tor funciona saltando de nodo en nodo, pero los nodos de salida necesitan tablas de enrutamiento.
- Con Mulvad + VPS + Monero es mÃ¡s seguro y prÃ¡ctico.
- Tor solo tiene sentido como capa adicional (hostear en .oninion, sin DNS pÃºblico).

### Sobre phishing y estafas

- Los estafadores no tienen conocimiento tÃ©cnico â†’ no usan VPN â†’ fÃ¡ciles de atribuir.
- IP Logger funciona porque la gente clic sin pensar.
- QRishing es la evoluciÃ³n: QR personalizado â†’ formulario falso â†’ credenciales robadas.
- "El cibercrimen ya no es de los hackers, ya no es de los que sabemos ciberseguridad informÃ¡tica."
- Herramientas de clonaciÃ³n: JS en consola (F12), Lovable, Emergent, GPT Image 2.
- Castillo la semana que viene enseÃ±arÃ¡ GoPhish + calentamiento de dominios + templates.

### Sobre RaaS

- El que develop el ransomware no se arriesga â€” vende software.
- El que compra acceso inicial tiene menos riesgo que desplegar ransomware.
- El intermediario (operador) es el que asume todo el riesgo.
- "Â¿TÃº para quÃ© compras un ransomware? Â¿Para comprar un cuadro digital y ponerlo en casa?"

### Sobre ciberresponsabilidad bancaria

- Los bancos tienen ciberresponsabilidad ligada a ciberseguro.
- Si adjuntas denuncia de policÃ­a + movimientos bancarios sellados â†’ el banco debe devolver el dinero.
- "Si en algÃºn momento sois vÃ­ctima de una ciberestafa... los bancos tienen ciberresponsabilidad."

### Sobre fuzzing

- Siempre enumerar primero: puertos â†’ directorios â†’ ficheros â†’ parÃ¡metros â†’ valores.
- Un PHP que devuelve 0 bytes puede estar ejecutÃ¡ndose en backend (sin printf).
- Cambio de cÃ³digo 500 a 200 = indicador de parÃ¡metro/valor vÃ¡lido.
- x8 es la herramienta estÃ¡ndar, pero Docker falla mucho â†’ Python o Cargo son mÃ¡s fiables.
- "No es lo mismo decirlo que hacerlo. Hay que practicar esta infraestructura."

---

## â‘© Comandos de la sesiÃ³n

```bash
# === DESCUBRIMIENTO ===
sudo netdiscover -r 10.0.2.0/24 # Descubrir IPs en el rango
ping 10.0.2.9 # Verificar conectividad + TTL

# === ENUMERACIÃ“N ===
nmap -sC -sV -p- <IP> --min-rate 1500 # Puertos + servicios
nikto -h <IP> # Vulnerabilidades del servidor
gobuster dir -u <IP> -w /usr/share/wordlists/dirb/common.txt
feroxbuster -u <IP> -x php,py,html,txt # Ficheros con extensiones

# === FUZZING DE PARÃMETROS ===
curl -X POST http://<IP>/index.php -d "test=1"
curl -X POST http://<IP>/index.php -d "backdoor=db.php"

# === INSTALACIÃ“N DE x8 ===
git clone <repo-x8>
cd x8
cargo build --release # InstalaciÃ³n con Cargo
# docker build -t x8 . # Alternativa Docker (poco fiable)

# === FUZZER PYTHON ===
python3 value_analyzer.py http://<IP>/index.php /usr/share/wordlists/dirb/common.txt

# === CONEXIÃ“N ANÃ“NIMA AL VPS ===
ssh root@<ip_del_vps> -i <ruta_a_la_key_privada>
```

---

## â‘ª RecapitulaciÃ³n integrada

Esta sesiÃ³n construye sobre el reconocimiento web de sesiones anteriores y abre dos frentes nuevos:

**Lado de cultura de atacante:**
- Infraestructura anÃ³nima en capas (Mulvad, VPS con Monero, dominio en Enjala, RSA sin contraseÃ±a).
- Crime as a Service como modelo de negocio con tres roles especializados y precios reales.
- EvoluciÃ³n del phishing hacia QRishing fÃ­sico.
- Ciberresponsabilidad bancaria como herramienta defensiva.
- TriangulaciÃ³n de seÃ±ales y geolocalizaciÃ³n por IP.

**Lado tÃ©cnico:**
- Cuando el directory listing y la inspecciÃ³n manual no revelan vectores de explotaciÃ³n, el siguiente paso es el **fuzzing de parÃ¡metros**: detectar quÃ© variables acepta un fichero PHP mediante comparaciÃ³n sistemÃ¡tica de respuestas HTTP.
- Herramientas: x8 (Cargo), fuzzer Python, curl para pruebas manuales.

> [!note] ConexiÃ³n
> Ambas habilidades â€” comprender al atacante y enumerar superficies no visibles â€” son parte del mismo ciclo metodolÃ³gico. La siguiente sesiÃ³n completarÃ¡ la explotaciÃ³n de la mÃ¡quina Rockstar a partir de las credenciales obtenidas, con Castillo enseÃ±ando GoPhish y calentamiento de dominios.

---

> [!note] Notas de clase
> - Esta clase fue impartida por **el coletas** (Carlos GÃ³mez Pintado).
> - El viernes viene **Dani** para hacer repaso de la semana.
> - La semana que viene viene **Castillo** para enseÃ±ar phishing con GoPhish y calentamiento de dominios.
> - Herramienta de OSINT de **Chris** estÃ¡ en su GitHub.
> - Se creÃ³ un QR de prueba para "El Goico" como ejercicio de QRishing.
> - El fuzzer en Python fue creado por **Asier** durante la clase y funcionÃ³ correctamente.
> - Se mencionÃ³ a **Nacho** como experto en anonimato (Trocador, Mulvad, Njala).
> - A saber quien tiene un colega en Toledo que hizo QRishing real en un restaurante.
> - La mÃ¡quina **Rockstar** se descargÃ³ en una sesiÃ³n anterior y se resolviÃ³ en ~20 minutos durante la clase.
> - Se mencionÃ³ la mÃ¡quina **Academy** (otra mÃ¡quina de HackerLabs, usada para auditorÃ­a WordPress).
> - **ProfPoint** se mencionÃ³ para campaÃ±as de phishing (pero es "un coÃ±azo").
> - **Vinted** se mencionÃ³ como ejemplo de scams: "vendÃ­an un peluche de 1.30m y 60 kilos por 30.000â‚¬."

â†’

â†’

â†’

â†’
